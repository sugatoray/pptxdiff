# SPEC.md — Feature Spec

What {{PROJECT_NAME}} does, feature by feature. Add a section per feature when it ships — describe *what* it does and *why* it matters to a user, not the implementation detail (that belongs in code comments or `WISDOM.md`).

No features have shipped yet — this file is a placeholder. Add your first section here right after you ship something.

<!--
## <Feature name>
What it does, from a user's perspective. A pointer to where it lives in the code
(`path/to/file.ext:line`) if that saves the next reader time.
-->
