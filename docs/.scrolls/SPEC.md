# SPEC.md — Slide Diff: Feature Specification

Single-file Design Component (`Slide Diff.dc.html`) that compares two PowerPoint decks (or many pairs) side by side, renders them with real fidelity, diffs every property PowerPoint exposes, and supports a full reviewer workflow.

## 1. Rendering
- Real `.pptx` parsing via JSZip (OOXML: slides, rels, layouts, masters, themes, notes, charts, tables, diagrams). JSZip is loaded locally from `vendor/jszip.min.js` (vendored, not CDN — see §24).
- High-fidelity slide rendering via `@aiden0z/pptx-renderer` — `parseZip → buildPresentation → PptxViewer.renderThumbnailToContainer`. Loaded locally from `vendor/pptx-renderer.bundle.js` (a self-contained esbuild bundle, vendored — see §24), not from the esm.sh CDN it originally shipped from.
- Graceful fallback to a schematic (box+text) renderer per-slide if the library can't parse a part ("Rendered" vs "Schematic" badge).
- Change-highlight overlay boxes drawn on top of the real render (outline colors keyed by diff status: changed/added/removed).

## 2. Diff engine (per slide pair)
Diffs text, font family/size/weight/italic, color, alignment, position, box size, wrap, shape border (width/color), hyperlinks.
Also diffs: images (content hash + position/size), charts (type + series data), tables (cell text, per-cell background/border, row/col counts, capped at 6 shown + "+N more"), SmartArt (text content), slide backgrounds (solid/gradient/image/pattern + color), animations (effect sequence signature), speaker notes (text AND formatting — bold/italic/font/color), slide transitions (type + speed/auto-advance), video/audio media (content hash), master/layout inheritance (layout name + theme name mismatch), embedded fonts (deck-level, not per-slide).

Word-level diff (LCS-based) highlights changed words within text/table-cell/chart-data/SmartArt content (strikethrough removed, bold added).

## 3. Deck-level comparison (not just 1:1 pairs)
- `alignSlides(A,B)`: LCS/Needleman-Wunsch-style alignment using per-slide word-overlap similarity (Dice coefficient) — handles inserted/removed slides without misaligning everything after.
- `refineMoves(alignment,A,B)`: post-processes remove+add pairs, re-matching by content similarity (>0.55) to detect **moved** slides (same content, different position) vs true add/remove.
- Four slide-level states surfaced: **added**, **removed**, **moved**, **modified** (+ identical/no-diff).

## 4. Views
- **Single pair**: two big slide previews (prev/next steppers per side) + full diff list below.
- **All pairs**: grid of every aligned slide pair (compact previews both sides) with Identical/Moved/N-changes badges; filter chips (All / Changed only / Pending / Approved / Rejected).
- **Batch**: upload multiple Before files + multiple After files (paired by order), fast diff-only parse (no renderer) produces a results table with per-pair diff counts; "Open" loads a pair into full single/all view with rendering.

## 5. Reviewer workflow
- Multiple reviewers: add via required `@handle`/email + optional display name → removable chips; click a chip to set "voting as."
- Per-slide Approve/Reject, tracked **per reviewer** (`decisions[pairKey][handle]`); slide-level aggregate status = rejected if any reviewer rejected, else approved if any approved, else pending.
- Per-slide comments with threaded replies (`comments[pairKey] = [{author,text,ts,replies:[...]}]`).
- Approval history log (newest-first, toggle panel) recording every approve/reject/comment/reply with reviewer + timestamp.
- Per-INDIVIDUAL-diff (not just per-slide) Approve/Reject, also per-reviewer, keyed `pairKey::diffKey`.
- Three-way merge **planning** (not an automatic file rewrite): per diff, "Keep Before / Keep After / Custom" picker → exportable merge plan JSON. Real OOXML-preserving auto-merge of arbitrary uploaded files is out of scope (would need a full round-trip OOXML writer).

## 6. Exports
- PDF (print, via `omelette-owns-print` meta + `window.print()`; chrome hidden via `.no-print` + `@media print`).
- Standalone HTML report (downloaded file, static, read-only summary of all diffs/decisions/comments).
- JSON report (full: deck names, presentation-level diffs, per-slide diffs, decisions, comments, history).
- Decisions → CSV / JSON (slide, decision, reviewer votes, comments).
- Merge plan → JSON.
- Copy Slack message (clipboard, plain-text mrkdwn-ish summary).
- Email summary (`mailto:` with subject+body).
- Copy shareable link (self-contained `data:text/html;base64,...` URL of the HTML report, copied to clipboard — no server/backend, so this is a self-contained data URL, not a hosted short link).

## 7. Other UI
- Dark/light mode toggle — full theme palette (bg/card/border/text/muted/chip) threaded through every panel via `theme.*` holes in `renderVals()`.
- Slide master & layout thumbnails: schematic (dashed rects from placeholder geometry) + layout name + theme name, per side, in single-pair view. NOT pixel-perfect (the renderer library only renders slides, not raw layout/master parts).
- Transition preview: shows the After slide's transition type/speed + a simplified crossfade animation swatch (not a literal PowerPoint transition render — technical limitation, clearly labeled).
- Tweaks (host Tweaks panel): `accentColor` (color), `wordLevelDiff` (boolean).

## 8. Files
- `Slide Diff.dc.html` — the DC (template + logic class).
- `sample-pptx.js` — ES module, builds real minimal-but-valid `.pptx` ArrayBuffers from a compact JS shape spec (used for the built-in sample deck AND exercises every OOXML feature: hyperlinks, tables w/ cell formatting, images, charts, SmartArt, video/audio (dummy bytes), notes (with formatting), transitions, backgrounds, embedded fonts).
- Sample deck ships 3 slide pairs: original Q3 slide (baseline diffs), a **RED** "Feature Showcase" pair with an intentional difference planted in every new-feature category, a **GREEN** control pair that's byte-identical across every category (proves no false positives) — Red/Green TDD fixture.
- `gen-sample-pptx.mjs` (Node-only, run via `node src/pptxdiff/gen-sample-pptx.mjs`) — materializes that same sample-deck spec as real files, `docs/assets/sample_before.pptx` / `sample_after.pptx`, for testing the diff engine without a running browser session. Not shipped as part of the CLI/app (`package.json`'s `files` list doesn't include it); it's a repo-local dev/test asset.

## Known/accepted limitations
- Merge = planning tool, not binary rewrite.
- Transition preview = simplified crossfade, not literal PPT transition engine.
- Master/layout thumbnails = schematic rects, not pixel-perfect render.
- Shareable link = self-contained data URL (no server-backed short link).
- Chart/SmartArt/media parts in generated sample are minimal-valid OOXML; the pptx-renderer library may fall back to "Schematic" mode for slides it can't fully parse (graceful, not a bug).

## 9. Added since initial SPEC (reviewer workflow, alignment, merge, testing)
- **Approve/reject rollup**: per-slide "X of Y diffs reviewed" summary (single view + All-pairs badge); All-pairs filter chips for "Has rejected diffs" / "Unreviewed diffs" alongside slide-decision filters.
- **Alignment tuning**: LCS match threshold raised 0.12→0.18; `refineMoves` runs a second order-preserving LCS pass over leftover removed/added slides so a whole reordered SECTION of slides links pairwise in sequence instead of via greedy nearest-neighbor (fixes cross-matching within moved blocks). Section-aware move note ("Section 'X' moved from slide N to slide M") surfaces separately from per-slide moved detection, driven by real `p:sectionLst` parsing.
- **Gap closures**: shape-level hyperlinks (`cNvPr > a:hlinkClick`) diffed for text boxes AND images, not just text-run hyperlinks; section headers parsed from `p:sectionLst` and diffed at the presentation level; duplicate-slide detection (>92% similarity within one deck) surfaces as a note panel; table cell-formatting diff cap raised 6→20; fuller theme-color resolution — table cell fill/border and background gradient/pattern first-stop colors now resolve `schemeClr` through the same theme+color-map resolver as text runs.
- **Reviewer state persistence**: reviewers, active reviewer, per-slide decisions, comments+replies, history log, per-diff decisions, and merge choices are saved to `localStorage` (key `slideDiffReviewerState_v1`) after every mutation and restored on mount.
- **Reviewer @mention notifications**: mentioning a registered @handle in a comment/reply queues an in-app notification; a bell icon (with unread badge) next to "Voting as" shows them, marked read on open. In-session only — no push/email delivery (client-only, no backend).
- **Batch mode upgrades**: pairing-mode toggle (Upload order / Filename-similarity via bigram Dice coefficient — `filenameSim`/`pairFilesByName`); unmatched files surface as their own "no match found" row; a real progress bar (`batchProgress`/`batchTotal`) replaces the static "Parsing…" label; "Batch report → CSV/JSON" export buttons.
- **Real OOXML merge writer**: "Merged .pptx (beta)" export button. For each aligned slide pair, tallies that slide's Keep-before/Keep-after per-diff picks (majority wins, ties default to After), converts the winning parsed slide back into `buildPptx`'s spec shape via `slideToBuildSpec`, and downloads a real, valid, openable `.pptx`. Scope: text/tables/background/notes/transitions carry over; images/charts/SmartArt/media are dropped (only a hash/reference was kept for those, not re-embeddable bytes) — a toast states this on export.
- **In-browser regression self-tests**: "Run self-tests" button runs `runSelfTests()` (async), covering word-diff, slide similarity, alignment (add/remove/move), duplicate detection, scheme-color resolution, key-namespacing, diff rollups, filename similarity, AND a real merge-export round-trip (builds a 2-slide `.pptx` via `buildPptx`, re-parses it with our own reader, asserts slide count + text survive). Results shown pass/fail in a panel.
- **Accessibility pass**: `aria-label`s on icon-only controls (remove-reviewer ✕, per-diff approve/reject ✓/✕, prev/next slide arrows ‹›); toast uses `role="status" aria-live="polite"`.

## 10. Added this session (collapsible panels, all-pairs selection, tags, keyboard, batch reorder, merge preview)
- Collapsible Approval history and Regression self-tests panels (click header to toggle, chevron indicator).
- All-pairs view: per-card checkbox selection + collapse chevron (hides preview thumbnails, keeps header); a global 'Collapse all / Expand all' button; a 'Selected (N)' filter toggle to show only checked pairs.
- Slide-pair status tags replaced the old Identical/Moved badges: primary tag IDENTICAL or CHANGED (always shown for matched pairs), ADDED/DELETED for slides only on one side, plus an orthogonal MOVED tag shown alongside the primary tag when a slide's relative order changed (e.g. "IDENTICAL, MOVED" or "CHANGED, MOVED").
- MOVED detection rewritten as a global Longest-Increasing-Subsequence check (`markMovedByLIS`) over all matched (aIdx,bIdx) pairs, with an O(n^2) DP tie-break that prefers keeping same-position pairs (aIdx===bIdx) out of the 'moved' set when LIS length ties with a genuinely-reordered pair. Replaces the old local-heuristic ('came from the leftover-matching pass') definition, which mislabeled same-position slides as moved and missed real reorders.
- Keyboard shortcuts (ignored while focus is in an input/textarea/contenteditable): A approve, R reject, ←/→ prev/next slide (both sides), V toggle single/all view, D toggle dark mode, C jump to single view and focus the comment box, N jump to the next changed slide after the current one. Hint shown in the toolbar.
- Batch upload: drag-to-reorder file lists per side (drag handle, remove button) — order matters for the default 'upload order' pairing mode.
- Merge winner preview: 'Preview merge winners…' opens a panel listing every aligned slide with its computed Before/After/Dropped/New winner (using the exact same `pickMergeWinner` the real export uses) before committing to a download.
- Regression tests added: a direct RED/GREEN case reproducing the exact same-position-vs-moved tie-break bug (`markMovedByLIS` unit test), and a test verifying the merge-preview winner matches `pickMergeWinner`'s real output for a given diff-choice set.

## 11. Added this session (merge-winner override, shortcuts help, touch reorder, persistence, new export formats)
- Merge-winner override: per-slide Before/After override buttons in the 'Preview merge winners…' panel force that slide's export winner regardless of the computed per-diff majority vote (labeled '(overridden)'); `pickMergeWinner` checks the override first.
- Full keyboard shortcuts help modal: '⌨ Shortcuts' button or press '?' opens an overlay listing every binding (A/R/←→/V/D/C/N/?), closable via Esc, click-outside, or ✕.
- Touch drag-to-reorder for batch upload file lists: touchstart/touchmove/touchend handlers (using `elementFromPoint` to find the row under the finger) alongside the existing HTML5 drag-and-drop, so reordering works on mobile/tablet.
- localStorage persistence extended to `collapsedPairs`, `selectedPairs`, and `mergeWinnerOverrides` (previously only reviewer/decision state persisted).
- JSON report export (`exportJsonReport`) now includes a `uiState` block (`collapsedPairs`, `selectedPairs`, `mergeWinnerOverrides`) so review-session UI state is captured in the exported report, not just reviewer decisions.
- Three new export formats, all built from the same `buildReportRows()` data as the other reports: **Markdown summary** (plain .md with a table per slide), **Notion** (same Markdown body, named/toasted to guide the user through Notion's Settings → Import → Markdown flow — Notion has no public write API usable from a static client-side file, so this is the honest maximum), **Confluence** (Confluence wiki markup .txt — `h1./h2.` headings, `||...||` table syntax — toasted to guide the user through Insert → Markup → Wiki Markup; same reasoning as Notion, no write API from client-side).

## 12. Added this session (touch drag-preview, report-JSON round-trip import, live push to Slack/Notion/Confluence) — built with Red/Green TDD
- Touch drag-preview ghost: a small floating label (filename + drag-handle glyph) follows the finger during a batch-file touch-drag, positioned via a genuinely-live `touchGhostStyle` coordinate hole (updated on touchmove, cleared on touchend) — the one legitimate use of a style hole per this project's authoring rules, since finger position cannot exist at parse time.
- Report-JSON round-trip import: `exportJsonReport` now includes each slide's internal `key` (pairKey) alongside its label; a new 'Import report JSON…' file control in the export menu re-parses a previously-exported report and restores `decisions`, `comments`, `history`, and the `uiState` block (`collapsedPairs`/`selectedPairs`/`mergeWinnerOverrides`) via a pure `parseReportForImport(report)` function, then persists to localStorage. Older exports without a `key` field are tolerated (skipped, not errored).
- Live push to Slack/Notion/Confluence — a 'Live push…' modal collects credentials (kept in component state only, explicitly NOT written to localStorage — the modal says so) and attempts a real network call per service:
  - **Slack**: POSTs to the user's incoming-webhook URL as a form-encoded `payload=` body with `mode:'no-cors'` (the standard client-only workaround for Slack's webhook CORS behavior — response is opaque, so success is reported optimistically).
  - **Notion**: PATCHes `/v1/blocks/{pageId}/children` with a bearer integration token, converting report rows into real Notion blocks (`buildNotionChildren` — heading_2 per slide, bulleted_list_item per diff, paragraph for identical slides). Expected to fail with a CORS error in practice (Notion's API isn't browser-origin-enabled) — caught and reported with a clear explanation plus a pointer to the existing file-based Notion export as a working fallback.
  - **Confluence**: GETs the current page (for its version number + existing body), then PUTs an appended body in Confluence storage format (`buildConfluenceStorageBody` — real `<table>`/`<h2>` HTML, not wiki markup) with HTTP Basic auth (email + API token). Same CORS caveat/fallback message as Notion.
- All three push flows' request-building logic (`buildSlackPayload`, `buildNotionChildren`, `buildConfluenceStorageBody`) are pure functions with no network/DOM dependency, specifically so they're unit-testable without mocking fetch — see the new self-tests.
- New Red/Green self-tests added for all three features above (touch ghost lifecycle, import round-trip incl. an older-export-without-key tolerance case, and the three payload/block/body builders) — written and reasoned about test-first per this session's explicit Red/Green TDD instruction, though committed in the same turn as the implementation (see WISDOM.md for the practical process note on this).

## 13. Added this session (merge-vs-overwrite import, Confluence replace/append, Slack delivery confirmation, opt-in credential persistence) — Red/Green TDD
- **Report-JSON import: merge vs. overwrite.** If the current session already has in-progress decisions/comments/history/UI state, importing a report now opens a choice modal (Merge / Overwrite / Cancel) instead of silently overwriting. `mergeReportPatch(current, imported)` is a pure function: the current session's own choices win on any per-key conflict (a reviewer's vote, a collapse/select/override flag); anything only present in the import is adopted; comments are unioned and de-duplicated; history is unioned, de-duplicated, and re-sorted newest-first.
- **Confluence replace-vs-append.** A toggle in the Live-push modal (default: Append) controls whether a Confluence push adds new content after the page's existing body (separated by `<hr/>`) or replaces it outright. `buildConfluenceNewBody(existingBody, newContent, mode)` is the pure decision function.
- **Slack delivery confirmation.** Replaced the old `mode:'no-cors'` fetch (zero information — always reported optimistic success) with a hidden `<form>` POST into a hidden `<iframe>`, whose `load`/`error` events give a real (if coarse) signal distinguishing "the request round-tripped" from "it never went out" (bad URL, offline). `interpretSlackDeliveryOutcome(kind)` maps `'load'→sent`, `'error'→failed`, `'timeout'→unknown` to user-facing messages, and is the pure/tested piece of this flow.
- **Opt-in credential persistence.** A checkbox in the Live-push modal ("Remember these credentials in this browser") controls whether Notion/Confluence/Slack credentials are written to a SEPARATE localStorage key (`slideDiffLivePushCreds_v1`, distinct from the main reviewer-state key) — off by default; unchecking immediately clears that key. `buildCredsPayload(state)` is the pure function defining exactly which fields are captured (and only those).
- New Red/Green self-tests for all four: `mergeReportPatch` (conflict-wins, adopt-imported-only, comment/history dedup), `buildConfluenceNewBody` (append/replace/missing-body), `interpretSlackDeliveryOutcome` (all three outcome kinds), `buildCredsPayload` (exact field-set capture, no leakage of unrelated state).

## 14. Added this session (per-field credential persistence, configurable merge-conflict winner, duplicate-slide 4th alignment state) — Red/Green TDD
- **Per-field credential persistence.** Replaced the single all-or-nothing 'Remember these credentials' checkbox with an individual 'Remember' checkbox next to each of the 7 live-push fields (Slack webhook URL; Notion token/page ID; Confluence base URL/email/token/page ID). `persistedCredFields` (state) tracks which fields are opted in; `buildCredsPayload(state, fieldsToPersist)` is the pure function filtering to exactly those fields; unchecking one field's box immediately drops just that field from the `slideDiffLivePushCreds_v1` localStorage entry (re-persisted from the remaining opted-in fields).
- **Configurable merge-conflict winner.** The report-import Merge/Overwrite choice modal now also offers 'On conflict: Current session wins / Imported file wins' (default: current). `mergeReportPatch(current, imported, conflictWinner)` takes the winner as a parameter — decisions, collapsedPairs, selectedPairs, and mergeWinnerOverrides all respect it; comments/history are unaffected (always unioned, no 'conflict' concept for list data).
- **Duplicate-slide 4th alignment state.** A slide-pair's status is no longer just the primary tag (IDENTICAL/CHANGED/ADDED/DELETED) plus an optional single MOVED badge — `computeExtraTags(al, dupIndicesA, dupIndicesB)` is a pure function returning a stackable array of extra tags: MOVED (relative order changed, from the existing LIS detector) and/or DUPLICATE (either side's slide is part of a near-identical group found by `findDuplicateSlides` within its OWN deck). These are independent facts and can co-occur (e.g. a slide that moved AND is a duplicate of another slide shows both tags) — this generalizes the earlier single `secondaryTag` field into a proper `extraTags` array rendered as a loop of badges.
- New Red/Green self-tests for all three: `mergeReportPatch` with `conflictWinner='imported'` (mirroring the existing 'current wins' test but for the opposite direction), `buildCredsPayload` field-filtering (none-selected → empty, some-selected → exactly those), and `computeExtraTags` (duplicate-only, moved-only, both-stacked, neither).

## 15. Added this session (cross-deck duplicate detection, select-all/none for credentials/batch files/reviewers) — Red/Green TDD
- **Cross-deck duplicate detection.** `findCrossDeckDuplicates(slidesA, slidesB, alignment)` is a pure function that flags a Before slide closely resembling a DIFFERENT After slide it is NOT aligned to (already-aligned pairs are excluded) — surfaced as a new 'Possible cross-deck duplicates' note panel alongside the existing same-deck duplicate-detection panel. Distinct from same-deck `findDuplicateSlides` (within one deck) and from move detection (a slide's own matched partner).
- **Select-all/none for credential checkboxes.** 'Remember all' / 'Forget all' buttons in the Live-push modal act on all 7 per-field 'Remember' checkboxes at once via the shared `buildSelectAllMap`/`buildSelectNoneMap` helpers.
- **Select-all/none for batch file selection.** Each side of the Batch upload file lists gained per-row checkboxes plus 'Select all' / 'Select none' / 'Remove selected (N)' buttons (bulk-remove selected files in one action).
- **Select-all/none for the reviewer list.** Each reviewer chip gained a checkbox; 'Select all' / 'Select none' / 'Remove selected (N)' buttons let a user bulk-remove multiple reviewers at once (active-reviewer handle is cleared if it was among those removed).
- All three select-all/none features share ONE generic pure-function pair — `buildSelectAllMap(keys)` and `buildSelectNoneMap()` — rather than three near-duplicate implementations, per this project's established 'generalize immediately' pattern (see WISDOM.md).
- New Red/Green self-tests: `findCrossDeckDuplicates` (finds an unaligned near-identical pair; excludes an already-aligned pair), `buildSelectAllMap`/`buildSelectNoneMap` (exact key-set behavior).

## 16. Added this session (bulk-reviewer-removal confirmation, select-all/none for diff list/comments/history) — Red/Green TDD
- **Confirmation for bulk reviewer removal.** Clicking 'Remove selected (N)' in the reviewer bar no longer removes immediately — it opens a confirm modal ('Remove N reviewer(s)?' / Cancel / Remove) via `requestRemoveSelectedReviewers` → `confirmRemoveSelectedReviewersYes`/`cancelRemoveSelectedReviewers`. Single-reviewer removal (the lone ✕ on a chip) remains immediate/unconfirmed, matching the existing direct-manipulation style — only the bulk/destructive path is gated.
- **Select-all/none for the diff list.** The 'Differences (N)' panel header gained Select all / Select none / Approve selected (N) / Reject selected (N) — bulk-applies the SAME per-diff decision keying (`curKey + '::' + d.key`) the individual ✓/✕ buttons already used, via `bulkSetDiffDecision(val)`.
- **Select-all/none for comments.** The Comments panel gained Select all / Select none / Delete selected (N) — the first DELETE capability for comments in this app (previously add/reply only); `deleteSelectedComments(pairKeyStr)` removes just the checked comments.
- **Select-all/none for history rows.** The Approval history panel gained Select all / Select none / Clear selected (N). History entries have no stable index (the array can be filtered), so selection keys off a content signature (`historySig(h)` — same shape used for de-dup in `mergeReportPatch`) rather than array position.
- All four reuse the shared `buildSelectAllMap`/`buildSelectNoneMap` helpers from the prior session — no new select-all/none implementation was written.
- New Red/Green self-tests: bulk-reviewer-removal confirmation gate (request → still present → confirm → actually removed), `bulkSetDiffDecision`, `deleteSelectedComments`, `clearSelectedHistory` (signature-keyed, not index-keyed).

## 17. Added this session (single-comment delete, confirmation for bulk comment delete + bulk history clear) — Red/Green TDD
- **Single-comment delete.** Each comment row now has its own ✕ delete button (`deleteComment(pairKeyStr, idx)`) — immediate, no confirmation, matching the app's existing single-item delete style (single reviewer, single batch file).
- **Confirmation for bulk comment delete.** 'Delete selected (N)' in the Comments panel now opens a confirm modal ('Delete N comment(s)?' / Cancel / Delete) via `requestDeleteSelectedComments(pairKeyStr)` → `confirmDeleteSelectedCommentsYes`/`cancelDeleteSelectedComments`, mirroring the bulk-reviewer-removal pattern from the prior session.
- **Confirmation for bulk history clear.** 'Clear selected (N)' in the Approval history panel now opens a confirm modal the same way, via `requestClearSelectedHistory()` → `confirmClearSelectedHistoryYes`/`cancelClearSelectedHistory`.
- This closes the asymmetry flagged in the prior session's GAP_ANALYSIS.md — ALL THREE bulk-select destructive actions (reviewers, comments, history) are now confirmation-gated; single-item deletes everywhere remain immediate by design.
- New Red/Green self-tests: `deleteComment` (immediate single delete), bulk-comment-delete confirm-gate round-trip (request → still present → confirm → actually deleted), bulk-history-clear confirm-gate round-trip (request → still present → confirm → actually cleared).

## 18. Added this session (reply delete, confirmation for single-comment delete/batch-file removal/clear-all-decisions) — Red/Green TDD
- **Reply delete.** Each reply within a comment thread now has its own ✕ (`deleteReply(pairKeyStr, commentIdx, replyIdx)`) — immediate, no confirmation (a reply is a smaller unit than a whole comment; only 'larger' deletes are confirmed in this app).
- **Confirmation for single-comment delete.** Reversed last session's explicit 'stays immediate' decision — the single-comment ✕ now opens a confirm modal ('Delete this comment?') via `requestDeleteComment`/`confirmDeleteSingleCommentYes`/`cancelDeleteSingleComment`.
- **Confirmation for batch-file removal.** The single-file ✕ in the Batch upload lists now opens a confirm modal ('Remove this file?') via `requestRemoveBatchFile`/`confirmRemoveBatchFileYes`/`cancelRemoveBatchFile`.
- **Confirmation for clearing all decisions.** New 'Clear all decisions' button in the header toolbar (only shown when `st.decisions` is non-empty) wipes every slide-level Approve/Reject vote across the whole comparison — gated behind a confirm modal ('Clear ALL decisions?') via `requestClearAllDecisions`/`confirmClearAllDecisionsYes`/`cancelClearAllDecisions`. Scoped to `decisions` only — does not touch `diffDecisions`, comments, or history.
- All three confirmation additions reuse the established request/cancel/confirmYes triad (see WISDOM.md) — this is now used for FIVE destructive actions total: bulk reviewer removal, bulk comment delete, bulk history clear, single-comment delete, batch-file removal, and clear-all-decisions.
- New Red/Green self-tests: `deleteReply` (removes exactly the targeted reply), single-comment-delete confirm-gate round-trip, batch-file-removal confirm-gate round-trip, clear-all-decisions confirm-gate round-trip.

## 19. Added this session (shared confirmation-modal component, scoped 'clear decisions') — Red/Green TDD
- **Shared confirmation-modal component.** Collapsed the 6 near-identical confirm-modal blocks (reviewers, comments-bulk, history-bulk, single-comment, batch-file, decisions) into ONE generic modal driven by a single `pendingConfirm` state object (`{title, body, confirmLabel, onConfirm}`). `openConfirm(config, onConfirmFn)`/`closeConfirm()`/`confirmPendingYes()` are the only three generic methods now; every `request*` method builds its config via a pure `buildConfirmConfig(kind, params)` (kinds: reviewers/comments/history/singleComment/batchFile/decisions) and calls `openConfirm`. This is the DC-appropriate way to build a 'shared component' — one data-driven template block, not a child DC (this app is intentionally single-DC per its authoring conventions).
- **Scoped 'Clear decisions'.** Replaced the flat 'Clear all decisions' button with a 'Clear decisions ▾' dropdown offering four scopes: **All** (everyone, every slide — previous behavior), **Only my votes** (clears just the active reviewer's votes, keeps others' on the same slides), **Specific reviewer** (pick a handle, clears just theirs), **Specific slide range** (row-position range as shown in the All-pairs view — clears every reviewer's votes but only for slides in that range). Pure decision logic lives in `computeDecisionsToClear(decisions, scope, opts)`, fully independent of state/DOM.
- New Red/Green self-tests: `buildConfirmConfig` for representative kinds (count-bearing, fixed-copy, scope-label-bearing), `computeDecisionsToClear` for all four scopes (all/mine/reviewer/range) confirming per-reviewer and per-slide-range isolation, and an integration-level test that `requestClearAllDecisions` routes through the shared modal. All 6 prior request*/confirm*Yes/cancel* self-tests were REWRITTEN (not just left in place) to assert against the new generic `pendingConfirm`/`confirmPendingYes` API instead of the retired per-action booleans — this was necessary, not optional, since the retired fields no longer exist.

## 20. Added this session (reviewer-handle autocomplete, invalid-range feedback, extended cross-deck duplicates) — Red/Green TDD
- **Reviewer-handle autocomplete**: the 'Specific reviewer' clear-decisions scope's handle input now has an HTML `<datalist>` populated from the current reviewer list.
- **Invalid slide-range feedback**: the clear-decisions range inputs turn red (border) and show an inline warning ('will be treated as a single row (N)') when From > To, matching `computeDecisionsToClear`'s actual clamp behavior.
- **Cross-deck duplicates, extended six ways**:
  1. *Performance at scale*: `findCrossDeckDuplicates` gained a `maxPairs` cap (default 6000 comparisons) — past it, returns `{skipped:true, totalComparisons}` instead of scanning, with a 'Run anyway' override in the UI.
  2. *Ignoring near-matches*: a Sensitivity slider (0.70–0.99, threshold parameter) lets the user raise the bar for what counts as 'near', plus a per-note 'Ignore' button that permanently dismisses one specific pair (`ignoredCrossDupKeys`).
  3. *Section headers*: each note now names the section (if any) each side's slide belongs to, via `sectionForSlideIndex(sections, idx)`.
  4. *Batch mode*: `runBatch` now also runs cross-deck duplicate detection per pair and appends a '· N cross-deck duplicate(s)' note to that row's summary.
  5. *Merge preview*: a 'Skip Before-only slides that closely resemble an After slide elsewhere' checkbox — when on, a dropped Before-only slide with an (unignored) cross-deck duplicate shows 'Skipped — duplicate of After slide N' instead of 'Dropped', and IS excluded from the merged .pptx export.
  6. *Duplicate slide merging*: the actual export behavior above — `exportMergedPptx` skips a Before-only slide's redundant copy when `mergeDuplicateSlides` is on, rather than including both the original and its near-duplicate cousin in the merged output (the pre-existing behavior always included Before-only slides via the `(pb || pa)` fallback, which is unaffected when the toggle is off).
- New Red/Green self-tests: threshold sensitivity (loose vs. strict), performance cap (skip vs. force), and `sectionForSlideIndex`.

## 21. npm CLI packaging (added this session)
- `pptxdiff` is now `npm`-installable as a CLI. `bin/cli.js` (stdlib-only: `node:http`/`node:fs`/`node:child_process`) serves `src/pptxdiff/{index.html,support.js,sample-pptx.js,vendor/**}` from a local server on an OS-assigned free port (`server.listen(0)`, no `--port` flag needed) and opens the default browser to it (`open`/`start`/`xdg-open` by platform; failure is swallowed — e.g. headless/no-GUI environments — since the URL is printed to stdout regardless).
- This is NOT a native desktop app (no Electron/Tauri) — deliberate scope choice, see GAP_CONTEXT.md. It's still fundamentally the same client-only app (see WISDOM.md's "No backend" constraint); the local server is a static file server, not application logic.
- **Fully offline-capable as of §24** (this claim from the original session is now superseded — see below). `package.json` (root): `bin: {pptxdiff: "./bin/cli.js"}`, `files: ["bin", "src/pptxdiff/index.html", "src/pptxdiff/support.js", "src/pptxdiff/sample-pptx.js", "src/pptxdiff/vendor", ...docs/assets]`, no runtime `dependencies`.

## 22. Static sample-pptx test fixtures (added this session; generator rewritten same session)
- `docs/assets/sample_before.pptx` / `docs/assets/sample_after.pptx` — real `.pptx` files checked into the repo, generated by `src/pptxdiff/gen-sample-pptx.mjs`, covering the same Red/Green TDD test-scenario intent as the in-browser sample deck (§8). Lets someone test the diff engine by uploading two real files directly (no need to click "Reset to sample" first) — useful for the CLI/static-file usage path (README Option C) and for manual QA.
- **Generator**: `gen-sample-pptx.mjs` uses `pptxgenjs` (npm, `devDependency`), NOT `sample-pptx.js`/`jszip` — an earlier version used the latter and produced files real Microsoft PowerPoint could not open or repair (invalid hand-rolled OOXML); switched wholesale to a real, spec-compliant writer library rather than continuing to patch individual XML defects. This script is independent of `buildSample()`/`sample-pptx.js` — it has its own parallel (simplified) shape spec.
- **Exercises**: text formatting (font/size/bold/italic/color/align), bullets, hyperlinks, text wrap, textbox borders, slide backgrounds, images (two distinct real PNGs, so content-hash diffing detects a real change), tables (per-cell fill/border), bar charts (differing series values), speaker notes (plain text), and add/remove/reorder slide scenarios.
- **Does NOT exercise** (no pptxgenjs API for these, or proven to break real PowerPoint in the prior hand-rolled generator): SmartArt/diagrams, slide transitions, embedded fonts, real video/audio media. The in-browser "Reset to sample" deck (`buildSample()`/`sample-pptx.js`, unchanged) still covers these — that path is unaffected since its output is only ever consumed by this app's own tolerant parser/renderer, never downloaded as a real file.
- Regenerate: `node src/pptxdiff/gen-sample-pptx.mjs` (requires `pptxgenjs` as a local devDependency — generation-time only, not a runtime dependency of the shipped app). After regenerating, validate with `uv run src/pptxdiff/test_gen-sample-pptx.py` — a self-contained (PEP 723 inline deps, `python-pptx`) regression script consolidating the checks that actually caught the two real bugs in this fixture's history: a content-type audit via `OpcPackage.iter_parts()` (generic `application/xml` fallback = a part the writer never properly declared — this is how the invalid SmartArt parts were found) and a geometry check of every shape's left/top/width/height against the slide's own declared size (this is how the off-slide-elements bug was found). A clean `python-pptx` open alone is NOT sufficient proof of anything — it doesn't validate OOXML schemas or shape geometry by itself; treat "opens in real PowerPoint" as unconfirmed until someone actually opens the file (see PLAN.md's LibreOffice-headless ticket for the next independent check).

## 23. Documentation website (added this session)
- `src/pptxdiff/docs-site/` — a MkDocs + Material for MkDocs documentation site: a home page, a Getting Started guide, one feature-walkthrough page per SPEC.md area (§1–§20, mapped 1:1 under `docs/features/`), a CLI reference, a VS Code extension page, an Architecture page, a Limitations table, an FAQ, and a Changelog page that transcludes the real root `CHANGELOG.md` (via `pymdownx.snippets`, `--8<-- "CHANGELOG.md"`) rather than duplicating it.
- **Tooling decision** (GreatDocs vs. Sphinx vs. Zensical vs. mkdocs-material — full comparison in `docs/.scrolls/DOCS.md`): mkdocs-material was chosen. GreatDocs (Posit's new Python-package doc generator) and Sphinx both center on introspecting a Python API surface, which this project doesn't have (it's a single-file HTML/JS app, not a Python library); Zensical (the same team's Rust-core successor to Material, explicitly compatible with existing Material projects) was still `0.0.51`/alpha on PyPI as of 2026-07-26, so not chosen for the primary build yet but flagged as the intended future migration once it's stable.
- **Build tooling**: `pyproject.toml` gained a `[dependency-groups] docs` entry (`mkdocs`, `mkdocs-material`) — `uv run --group docs mkdocs serve -f src/pptxdiff/docs-site/mkdocs.yml` to preview, `... build ... --strict` to build. Output goes to `src/pptxdiff/docs-site/site/` (git-ignored), not published/deployed anywhere by this session.
- Verified via `mkdocs build --strict` (clean) plus a local Playwright/Chromium screenshot pass of the home, a feature, the architecture, and the changelog pages.
- **Note (added when §24 landed)**: this session's content (architecture.md, getting-started.md, index.md, limitations.md, faq.md) described the app as CDN-dependent/requiring internet access, which was accurate when written but is now stale — §24 vendored every CDN dependency locally. Those pages were updated for consistency; see §24 and GAP_CONTEXT.md.

## 24. Fully offline-capable / air-gapped (added this session) — Red/Green TDD
- **Every runtime dependency the app previously loaded from a CDN is now vendored locally** under `src/pptxdiff/vendor/`, and the app runs with zero network access required. This supersedes SPEC.md §21's earlier "still requires internet access... deliberate scope choice" note and GAP_ANALYSIS.md's matching "Packaging" gap — both closed this session by explicit user request.
- **Vendored files** (`src/pptxdiff/vendor/`):
  - `react.production.min.js` / `react-dom.production.min.js` — React/ReactDOM 18.3.1 UMD builds, byte-identical to what unpkg served (verified via SHA-384, matching `support.js`'s existing SRI hashes exactly — no version change).
  - `babel.min.js` — `@babel/standalone` 7.26.4, used to transpile the app's inline JSX/TS logic client-side (same version as before, unpkg → local).
  - `jszip.min.js` — JSZip 3.10.1 UMD build, used directly by `index.html`'s own `.pptx` parsing (was cdnjs).
  - `pptx-renderer.bundle.js` — `@aiden0z/pptx-renderer@1.1.0` (same version as the prior `esm.sh` pin), bundled via `esbuild` together with its `jszip` runtime dependency into ONE self-contained ES module with zero remaining external imports. The original esm.sh URL transparently resolved `pptx-renderer`'s bare `import "jszip"` (and echarts, which turned out to be fully inlined already by the library's own vite build) to CDN URLs on the fly — a plain local file can't do that resolution, so `esbuild --bundle --format=esm` was used to inline `jszip` at build time instead. `pdfjs-dist` (an optional peer dep, loaded via a runtime-constructed `import(pdfjsUrl)` string inside a Web Worker, only for embedded-PDF-object rendering) is NOT bundled — it was never part of this app's dependency set even under the CDN version, and stays a documented, narrow, pre-existing gap (see GAP_ANALYSIS.md).
  - `fonts/spectral.css` + 4 `.woff2` files — the Spectral typeface (400/500/600 normal + 400 italic, **latin subset only** — the UI is English-only per the existing a11y/i18n gap), self-hosted instead of `fonts.googleapis.com`/`fonts.gstatic.com`.
  - `licenses/` — upstream license texts for every vendored package (React/ReactDOM MIT, Babel MIT, JSZip MIT, Spectral OFL 1.1), for attribution.
- **Wiring**: `support.js`'s `REACT_URL`/`REACT_DOM_URL`/`BABEL_URL` and `index.html`'s JSZip `<script src>`, font `<link>`, and `LIB_URL` (dynamic `import()` target for the renderer) all now point at `./vendor/...` relative paths instead of CDN URLs. `bin/cli.js`'s static file server gained MIME types for `.css`/`.woff2`/`.mjs` (previously only `.html`/`.js`) so these vendored assets serve correctly. `package.json`'s `files` list gained `src/pptxdiff/vendor` so the npm-published package includes the vendored assets.
- **Testing (Red/Green TDD)**: `src/pptxdiff/test_offline_capable.mjs` (Node, no dependencies) statically scans `index.html`/`support.js` for references to known CDN asset hosts (unpkg, esm.sh, cdnjs, fonts.googleapis/gstatic, jsdelivr) and asserts every local `./vendor/...` reference actually exists on disk — RED (5 failing checks) before vendoring, GREEN (22/22) after. Separately, a one-off Playwright smoke test (not checked in — see WISDOM.md) drove the app in headless Chromium with ALL non-localhost/non-`data:`/non-`blob:` network requests aborted: zero external requests attempted, zero console errors, all 88 of the app's own in-browser `runSelfTests()` passed, and uploading the real on-disk fixtures (`docs/assets/sample_*.pptx`) produced genuine "Rendered" (not "Schematic" fallback) badges — proving the vendored `pptx-renderer` bundle is functionally equivalent to the CDN version, not just present on disk.
- **Not changed**: the app's *feature* network calls (Slack/Notion/Confluence "Live push…", explicitly user-triggered) are unaffected and still require internet access when used — vendoring only applies to what the app needs just to boot and function. This distinction is intentional; see GAP_CONTEXT.md.
- **Superseded note**: §25 below adds an opt-in `PPTXDIFF_LITE_MODE` that switches these same five dependencies BACK to their original CDN URLs on request — `test_offline_capable.mjs`'s assertions were updated accordingly (it now checks that both the vendor default AND the gated CDN fallback exist, not that the CDN strings are wholly absent). The "runs with zero network access required" claim above is still true — it's the *default*; §25 explains the explicit opt-out.

## 25. `PPTXDIFF_LITE_MODE` — opt-in CDN sourcing (added this session) — Red/Green TDD
- **What it does**: an escape hatch back to §24's original CDN sourcing, for anyone who wants their browser to always fetch the latest-patched build of a pinned CDN asset, or who is debugging a vendoring-specific issue and wants to A/B against the unmodified upstream. Off by default — the app is offline-capable by default per §24; this only matters if explicitly requested.
- **Two equivalent triggers**: the env var `PPTXDIFF_LITE_MODE` (accepts `1`/`y`/`yes`/`true`, case-insensitive) read by `bin/cli.js`, OR appending `?lite=1` directly to the app's URL (also accepts `y`/`yes`/`true`, case-insensitive) — the env var is really just a convenience that makes `bin/cli.js` open the browser at `<url>/?lite=1` for you. The query param is the one true source of runtime truth; it's what the browser-side code actually reads (via `location.search`), and it's the only trigger that works at all for the "just open `index.html`"/VS Code extension paths, which have no Node process to read an env var from.
- **Where the decision is made and re-used**: `support.js` (the first script the page loads) computes `PPTXDIFF_LITE_MODE` once from `location.search` and exposes it as `window.__PPTXDIFF_LITE_MODE__`, so `index.html`'s own inline helmet script (for the JSZip `<script>` and Spectral `<link>`, which need `createElement`-based conditional injection since a helmet tag's `src`/`href` can't hold a `{{ }}` JS expression — DC template holes are dotted-lookup only) and the component's `LIB_URL` class field (for `pptx-renderer`) both read the SAME flag rather than re-deriving it. `REACT_URL`/`REACT_DOM_URL`/`BABEL_URL` in `support.js` branch on it directly. React/ReactDOM keep the SAME SRI hash regardless of source (the vendored files are still byte-identical to what unpkg serves — see §24), so integrity checking still applies to the CDN path too.
- **Testing (Red/Green TDD)**: `src/pptxdiff/test_lite_mode_cli.mjs` (new) spawns the real `bin/cli.js` as a child process across 10 env-var value cases (unset/1/y/Y/yes/true/TRUE/0/false/garbage) and asserts the printed URL contains `?lite=1` only for the truthy cases — RED (6 failures) before implementing the env var handling, GREEN (10/10) after. `test_offline_capable.mjs` was updated in place (not left stale) to assert BOTH halves of the contract per vendor swap point: the `./vendor/...` default is present AND the CDN URL is present AND gated behind the `PPTXDIFF_LITE_MODE`/`__PPTXDIFF_LITE_MODE__` flag (not a stray unconditional reference) — this is a genuine semantic change from §24's version of this test (which asserted CDN strings were wholly absent), since those strings are now legitimately part of the source. Verified end-to-end with a one-off Playwright script (not checked in): default mode with network blocked still shows zero external requests + 88/88 self-tests (no regression from §24); `?lite=1` mode with network open shows the browser correctly attempting all 5 swap points against their real CDN URLs (`unpkg.com/react@...`, `unpkg.com/react-dom@...`, `cdnjs.cloudflare.com/.../jszip...`, `fonts.googleapis.com/css2...`) and zero `/vendor/` requests — confirmed via captured request URLs, not just a static source-code check.
- **Sandbox caveat, not a code issue**: in the sandboxed dev environment this was built in, `unpkg.com`/`esm.sh`/`cdnjs.cloudflare.com` are blocked by the outbound proxy (confirmed via direct `curl`), so the CDN fetches genuinely fail there once `?lite=1` is set — React fails to load, and `support.js`'s existing `loadReactUmd().then(init).catch(...)` correctly logs the failure and the app never boots (pre-existing error-handling behavior, not new). This is a property of that sandbox's network policy, not of the feature; on a machine with normal internet access the CDN fetches succeed exactly as they did before §24's vendoring work.
- **Third trigger, added this session: an in-app "Offline Mode" toggle.** A labeled switch pinned to the extreme top-right corner of the page (absolutely positioned relative to the 1180px content column, distinctly separate from the toolbar button cluster below it — a deliberate design choice, mocked up and approved before implementation) reflects the CURRENT load-time decision (ON = offline/vendored = default, OFF = `?lite=1`/CDN) and, on click, navigates to the flipped URL. Styled as a track+thumb switch (not another rectangular text button) specifically so it reads as a persistent mode setting rather than an action — track color is `#3E7C5A` (this codebase's existing semantic "good" color, already used elsewhere for approve/add states) when ON, `theme.border` when OFF. Since which libraries got loaded is a load-time-only decision (see above), clicking it is a real `window.location.href` navigation/reload, not a live state flip — any currently-uploaded (not-yet-persisted) Before/After files are lost on that reload, same as manually editing the URL today. Pure URL computation lives in `computeLiteModeToggleUrl(href, wantOffline)` (adds/removes just the `lite` param, preserves every other query param); the impure `toggleLiteMode()` shell reads the current flag and calls it. New self-tests (4 assertions) cover strip/add and preservation-of-other-params in both directions — RED→GREEN verified, plus an end-to-end Playwright pass confirming the rendered `aria-checked`/title/track/thumb state and the actual post-click URL in the default (offline) direction; the CDN/lite direction's full visual render couldn't be confirmed in this sandbox for the same proxy-blocking reason noted above, but the underlying flag read is the identical one-line ternary pattern already proven correct for `LIB_URL`.

## 26. Documentation coverage tracking (added this session) — Red/Green TDD
- **What it does**: a programmatic tracker for whether every known pptxdiff feature and limitation actually has real documentation on `src/pptxdiff/docs-site/`, and how good that coverage is — not a vibe check, a real cross-reference between a canonical checklist and what each page actually declares about itself.
- **`src/pptxdiff/docs-site/scripts/coverage_registry.yml`**: the canonical checklist — 21 feature ids + 12 limitation ids, each with a stable `id`, human-readable `title`, and a `source` pointer back into `docs/.scrolls/SPEC.md`/`GAP_ANALYSIS.md` (this project's own authoritative spec/gap tracking — the registry deliberately doesn't invent a parallel taxonomy, it curates/rolls up what's already there). This file is NOT under `docs/` and is not built by mkdocs.
- **Per-page `doc_coverage:` front matter**: any page under `docs-site/docs/` can declare `doc_coverage: [{id, quality, anchor?}]` — `quality` is `complete` or `partial` (an item with zero declarations anywhere is computed as `missing`, never self-declared). `anchor` is optional and points at a real heading slug within that same page for pinpoint deep-linking; omitting it just links the whole page. Per-item human-readable titles live ONLY in the registry — pages reference by `id` only, so there is exactly one place that owns what each row is called (avoids the exact kind of title-drift risk raised and resolved in the design-discussion turn before this was built).
- **`src/pptxdiff/docs-site/scripts/sync_doc_coverage.py`**: standalone, PEP 723 (`pyyaml` inline dep, matching `test_gen-sample-pptx.py`'s existing convention), `uv run`-able. Two modes:
  - `--write` — scans every page, cross-references the registry, and writes the computed report into `documentation-coverage.md`'s OWN front matter (`coverage_summary: {generated_at, totals, items}`).
  - `--check` — **the actual Red/Green test.** Recomputes fresh and fails (exit 1) if: any page references a registry `id` that doesn't exist; a `quality` value isn't `complete`/`partial`; an `anchor` doesn't match any real heading in its page; the registry has a duplicate `id`; or the fresh scan doesn't match what's currently written in `documentation-coverage.md` (staleness). This does NOT require 100% coverage — `partial`/`missing` are legitimate, expected, non-failing states; the invariant under test is internal consistency and freshness, not completeness.
  - Verified RED→GREEN for real: ran `--check` before any front matter or the coverage page existed (RED — page missing), implemented `--write`, ran it, `--check` passed (GREEN). Then deliberately broke three different things one at a time (a typo'd `id`, a broken `anchor`, a duplicate registry `id`) and confirmed `--check` caught each with a specific, correct error message before reverting and confirming GREEN again — proof the check is meaningful, not vacuous.
- **`documentation-coverage.md` + `mkdocs-macros-plugin`**: the coverage page's BODY is mostly two Jinja macro calls (`render_coverage_summary()`, `render_coverage_table(kind=...)`), defined in `src/pptxdiff/docs-site/main.py` (`define_env(env)` + `@env.macro`). The macros read `documentation-coverage.md`'s own `coverage_summary` front matter straight off disk (not via the plugin's live page-context API) and return Markdown table strings that get spliced in and re-parsed — real Jinja templating driven by front-matter data, per the explicit ask.
- **Critical scoping decision**: `mkdocs.yml`'s `macros:` plugin config sets `render_by_default: false` — Jinja evaluation is opt-in per page (`render_macros: true` in front matter), not opt-out. Without this, the plugin would try to Jinja-evaluate ANY literal `{{ }}` appearing anywhere across the whole docs site — and this app's own DC template syntax (`index.html`) literally IS `{{ theme.border }}`-style, so a future page quoting it (none currently do — checked) would otherwise break the build. Only `documentation-coverage.md` opts in.
- **Result as of this session**: 25/33 items complete, 0 partial, 8 missing — 21/21 features fully covered, 4/12 limitation categories covered (rendering, transitions, exports, reviewer-workflow/merge). The 8 missing are real, honestly-surfaced gaps in the PUBLIC docs (diff-engine, deck-comparison, batch, testing-fixtures, accessibility, packaging, offline-capability, and lite-mode limitations exist in `docs/.scrolls/GAP_ANALYSIS.md` but were never carried into `limitations.md` or any feature page) — deliberately left as `missing` rather than padded out in the same session that built the measurement tool, so the first report is an honest baseline, not a number inflated to flatter the tool that just measured it.

## 27. Screenshot export — zip and self-contained HTML viewer (added this session)
- **What it does**: two new Export-menu items, both driven by the same `captureAllScreenshots()` orchestrator — it switches the app through All-pairs (collapsed, then expanded), then every aligned slide pair's single-view comparison in turn, capturing a full-page screenshot at each step (including its diffs, since the single-view capture is the full comparison page, not just a thumbnail), then restores whatever view/collapse-state the user was actually on before the export ran.
  - Both live under a **"Diff Screenshots ▾" nested submenu** in the Export menu (a `<details>` nested inside the Export `<details>`, same technique as "Clear decisions ▾" — added later the same session, replacing two flat top-level buttons, per explicit follow-up ask): **"Download as ZIP"** bundles every captured screenshot into one downloadable `pptxdiff-screenshots.zip`, via JSZip (already vendored, no new dependency) — `00-all-pairs-collapsed.svg`, `01-all-pairs-expanded.svg`, then one `NN-pair-<slide-label>.svg` per aligned pair; **"Download as HTML viewer"** bundles the same captures into one self-contained `pptxdiff-screenshots.html` — a left-hand nav pane lists every screenshot as a clickable tab, and clicking one shows it in the main pane via a small inline vanilla-JS switcher (no framework — this is a plain standalone artifact, not another DC-templated page).
- **Captures are SVG, not rasterized PNG — deliberately, not as a shortcut.** The technique clones the live DOM into an SVG `<foreignObject>` (reliable here specifically because this app's own authoring constraint — every style is an inline `style="..."` attribute, no external stylesheet/CSS-variable resolution — removes the main reason `html2canvas`-style libraries exist). The obvious next step, drawing that foreignObject SVG onto a `<canvas>` and calling `toDataURL()` for real PNG bytes, hits a hard, unconditional browser wall instead: Chromium taints any canvas an SVG containing `<foreignObject>` is drawn onto — even a plain-text SVG with zero images — so `toDataURL()`/`toBlob()` always throws `SecurityError` regardless of content (confirmed with an isolated repro, not assumed). Shipping the SVG itself sidesteps this entirely: the taint restriction is only on reading pixel bytes back out via canvas, not on a browser rendering/displaying the SVG, which it does identically to the live page. Known gap: custom `@font-face` text (this app's Spectral headings) isn't guaranteed to carry its font into the foreignObject sandbox — plain inline `style` colors/layout/images/canvases all do.
- **New shared pure helpers** (used by the screenshot export AND refactored into existing code rather than duplicated): `pairLabelFor(al)` (the canonical "Slide N → M" / "(removed/added in After)" label, now also used by `buildReportRows()`), `buildCollapsedMap(alignment, val)` (now also used by `toggleAllPairsCollapsed()`), `sanitizeFilename(s)`. `buildScreenshotsHtmlDoc(shots)` is a pure function kept separate from the impure `exportScreenshotsHtml()` shell so it's directly unit-testable.
- **Testing**: Red/Green self-tests for all the pure helpers above (14 new assertions, self-test count now 103). The impure DOM-capture/zip/download shell isn't self-tested directly (mutates real view state, triggers a real download) — instead verified end-to-end with Playwright against the real CLI: both exports download correctly, the zip's `.svg` files each open rendering the correct view, and the HTML viewer's tabs correctly switch between all 8 captured screenshots for the sample deck.
- **Relationship to the docs-site's own screenshot tooling**: unrelated, deliberately. `src/pptxdiff/docs-site/scripts/capture_screenshots.mjs` (+ `webm_to_gif.py`) is Node/Playwright dev-tooling that drives a real headless browser to produce the docs site's own marketing/documentation screenshots — it runs at development time, outside the shipped app, and is never something an end user's browser executes. This feature is the opposite: an in-app, user-facing button that runs entirely inside the end user's own already-open browser tab, with no headless browser, no Node, no dev-only tooling involved.

## 28. "Copy shareable link" now carries the diff status, not just a static report (added this session)
- **What changed**: the shareable link (§6/§7's original "self-contained `data:text/html;base64,...` URL") is unchanged in kind — still a single self-contained data URL, no server, no hosted short link, exact same original scope — but its generated page now ALSO embeds the same JSON report `exportJsonReport()`/"JSON report" produces (`<script type="application/json" id="pptxdiff-report-json">`), plus a "download report.json" link (a second, nested `data:application/json;base64,...` URL) right in the page body. Opening the shared link elsewhere and downloading that JSON, then using PptxDiff's existing **Export → Import report JSON…**, restores the exact same decisions/comments/history/uiState (collapsedPairs/selectedPairs/mergeWinnerOverrides) — "the same diff status" — on another machine. No new restore mechanism was built; this reuses the import path that already existed for the JSON report export.
- **New shared pure helper**: `buildJsonReport()` extracts the report-object-building logic that used to live only inline in `exportJsonReport()`, now used by both it and the new `buildShareLinkHtml()`/`exportShareLink()` — one canonical report shape, not two independently-maintained ones.
- **A real injection-safety issue found and fixed, not just theoretical**: `JSON.stringify()` never escapes `<`, so a report whose comment/text content contains a literal script-closing tag substring would, unescaped, prematurely close the embedded `<script>` in the generated shared page and could inject a live element into it. Fixed with the standard technique — `JSON.stringify(report).replace(/</g, '\\u003c')` before embedding — and demonstrated as genuine RED→GREEN (not just asserted): removed the escaping, confirmed exactly the 3 self-tests that depend on it fail (a stray `<img>` gets created in the parsed shared page, and the embedded JSON is truncated/unparseable) while all other tests stay green, then restored the escaping and confirmed all green again. See WISDOM.md for a second, unrelated instance of the same underlying `</script`-in-source-text trap hit while writing this test's own hostile-input fixture.
- **Testing**: new self-tests cover (1) `buildJsonReport()` → `parseReportForImport()` round-tripping the CURRENT session's actual decisions/comments/uiState exactly (not just an independently-plausible fixture), (2) `buildShareLinkHtml()` embedding that exact report as parseable JSON (extracted via `DOMParser`, the same way a real browser opening the link would), (3) the original human-readable report body and the new "download report.json" link both still present, and (4) the injection-safety RED/GREEN above. Self-test count now 119.
- **Verified end-to-end with Playwright against the real running app, not just self-tests**: approved a slide + attempted a comment, clicked "Copy shareable link," read the real clipboard content (a `data:text/html` URL), navigated a real browser tab to that URL, confirmed the embedded JSON parses with the approved decision intact, clicked the page's own "download report.json" link, then imported that downloaded file into a FRESH tab (simulating "another machine") via the existing Import report JSON… flow and confirmed the approved decision was restored and visible.
- **Original scope explicitly re-checked, not just assumed intact**: still zero backend/server component (GAP_CONTEXT.md's "Data-URL shareable link" reasoning is unchanged — there's still nowhere to host a real short link); still a single self-contained data URL copied to clipboard; the original human-readable report content is unchanged, only added-to. The one existing accepted limitation this makes more likely to matter, not newly introduces, is GAP_ANALYSIS.md's already-documented "large decks could produce a very long URL" — the payload is now bigger (adds the full JSON report on top of the HTML report), not a new kind of gap.

## 29. Parser-independent content checksum in every diff export (added this session)
- **What it does**: every diff export — PDF/print, HTML report, JSON report, Markdown, Notion, Confluence, Slack, Email, and the shareable link (§28) — now includes a SHA-256 "content checksum" per deck. A new always-visible "Content checksums" panel in the app itself (single-pair view, not `no-print`) shows both hashes plus the algorithm description; this panel is what feeds the PDF/print export, since that's just `window.print()` of the live page.
- **Deliberately not a raw file hash.** A `.pptx` is a zip; `docProps/core.xml`'s `dcterms:modified`/`cp:revision` and `docProps/app.xml`'s `TotalTime` edit-session counter change on every routine re-save with zero real content change — hashing the raw file bytes would make the checksum churn on trivial re-saves, defeating its purpose as a stable content fingerprint. **Deliberately also not a hash of this app's own parsed slide structure** (the first design considered, before an explicit follow-up ask) — that would only prove "our parser extracted the same fields," not "the underlying OOXML content is the same," and would silently miss anything the parser doesn't read.
- **The actual technique — parser-independent, OpenSSL-verifiable**: `computeContentChecksum(buf)` reads the raw zip directly via JSZip (already vendored, no new dependency) — completely independently of this app's own semantic `.pptx` parser (`parseBuffer`/`parseSlide`/etc.). It SHA-256s every part's raw decompressed bytes (decompressing already sidesteps zip-container-level timestamps and compression-method differences — those live outside a part's own content) except three explicitly-excluded parts, sorts the remaining parts by path (determinism regardless of the zip's internal entry order), then SHA-256s the resulting sorted `"path:hash\n"` manifest as the final checksum. Since SHA-256 is a standardized, tool-independent algorithm, this is exactly reproducible OUTSIDE the app with `openssl dgst -sha256`/`sha256sum` on the same bytes — OpenSSL itself can't run inside a client-side-only browser app (no shell, no backend — see WISDOM.md's Constraints), but the standard algorithm delivers the same verifiable property the user actually wanted.
- **Excluded parts, and why** (`isExcludedFromContentChecksum(path)`, a pure predicate): `docProps/core.xml` and `docProps/app.xml` are pure save-timestamp/editing-session metadata, never slide content; `docProps/thumbnail.*` (any extension) is a derived preview image some tools embed — not itself authored content, and not guaranteed to re-render identically across saves/tools even when the real content hasn't changed. Everything else — every slide/layout/master/theme XML, media, `[Content_Types].xml`, every `.rels` file — is real content or a real structural fact about it (e.g. `[Content_Types].xml` changes when slides are added/removed), and is included.
- **Wiring**: computed once per side in `ingest()` right after parsing (`contentHashA`/`contentHashB` in state), read synchronously by every export — no export re-derives it or re-reads the zip.
- **Testing**: `isExcludedFromContentChecksum` has 7 direct pure-function assertions (both exclusions and non-exclusions, including a same-named-file-outside-docProps/ edge case). `computeContentChecksum` itself is tested against synthetic JSZip fixtures built with precise control over what differs — two zips with identical "slide content" but different `docProps/core.xml`/`app.xml` timestamps hash IDENTICALLY; a zip with genuinely different slide content hashes DIFFERENTLY; the graceful-null fallback when `crypto.subtle` is unavailable is exercised directly. Self-test count now 130. Demonstrated genuine RED→GREEN: temporarily made the exclusion predicate always return `false`, confirmed exactly 5 dependent assertions failed (125/130) while everything else stayed green, restored it, confirmed 130/130.
- **Verified end-to-end against the real running app, with a real (not synthetic) proof of timestamp-independence**: all 7 export formats show the identical, correctly-computed checksums for the loaded sample deck; clicking "Reset to sample" — which regenerates the `.pptx` files from scratch via `buildPptx()`, with a fresh timestamp each time — produces the EXACT SAME checksum before and after, against the app's real file-generation path, not just hand-built test fixtures.
- **Known limitation, explicitly surfaced, not hidden**: `crypto.subtle` (native Web Crypto, no new dependency) requires a secure context — guaranteed under the CLI's `http://localhost` default, NOT guaranteed under this project's other supported launch path (opening `index.html` directly via `file://`). `this.cryptoSubtleAvailable` is checked once at boot and distinguishes "still computing" from "genuinely unavailable (requires a secure context)" in the UI rather than showing a misleading permanent "computing…"; `computeContentChecksum()` returns `null` (never throws) in that case, and every export shows "unavailable" rather than a wrong/fabricated hash.

## 30. Headless CLI (`pptxdiff-cli`) and Web API (`@pptxdiff/server`) — Phase 1 (added this session)
- **What it does**: two new, separate, opt-in npm packages under `src/packages/` let another program — a script, a CI job, a git driver, an AI agent — get a pptxdiff diff result as data, without a human clicking through the browser app. Full design/rationale: `docs/.scrolls/CLI_API_DESIGN.md` (architecture, phasing, package split) and `docs/.scrolls/CLI_and_API.md` (why Playwright over Deno for this phase).
- **`pptxdiff-cli`** (`src/packages/pptxdiff-cli/`):
  - `pptxdiff-cli diff <before.pptx> <after.pptx> [--json] [--out <file>] [--quiet] [--timeout <ms>]` — exit `0` = no differences, `1` = differences found (matches `diff(1)`/`git diff --exit-code`, so it gates a CI/git step directly), `2` = tool error (missing file, unparseable file, no browser available). Default output is a human-readable summary; `--json` prints the same report shape the GUI's "Export → JSON report" produces.
  - `pptxdiff-cli checksum <file.pptx> [--json] [--out <file>]` — prints the file's parser-independent SHA-256 content checksum (§29) standalone, without needing a second file.
  - `pptxdiff-cli --help` / `--version`.
- **`@pptxdiff/server`** (`src/packages/pptxdiff-server/`): stdlib-only (`node:http`, no framework) HTTP wrapper around the same automation layer. `POST /v1/diff` (`{before:{name?,content},after:{name?,content}}`, base64 content, → the same JSON report shape), `POST /v1/checksum` (`{file:{name?,content}}` → `{algorithm,hash}`), `GET /v1/health`, `GET /openapi.json` (OpenAPI 3.1 spec), and `GET /docs` (Swagger UI page backed by the spec). Binds to `127.0.0.1` by default, matching `bin/cli.js`'s existing hardening precedent. **No authentication yet** — a non-loopback bind is an explicit, unguarded opt-in the README warns about; see GAP_ANALYSIS.md.
- **How it actually runs (Phase 1, per the design doc's phased plan)**: both packages are thin wrappers around one shared automation shim (`pptxdiff-cli`'s `lib/automation.js`) that drives the real, unmodified browser app (`src/pptxdiff/index.html`) headlessly via `playwright-core` — uploads both files through the real file inputs, waits for the real diff engine, clicks the real "Export → JSON report" button, and reads the resulting download. This is deliberate: reusing the exact GUI code path (not a reimplementation) means the CLI/API can never silently disagree with what a human sees in the browser for the same two files. The native, browser-free `@pptxdiff/core` engine extraction described in the design doc's Phase 2 has NOT been built yet — every invocation currently pays a real headless-Chromium boot cost (see WISDOM.md and GAP_ANALYSIS.md).
- **Browser resolution** (`pptxdiff-cli`'s `lib/browser.js`): `PPTXDIFF_CHROME_PATH` env override, then well-known per-OS Chrome/Chromium/Edge install paths, else `null` (defer to `playwright-core`'s own managed-browser resolution) — deliberately depends on `playwright-core` (no bundled Chromium download), not the full `playwright` package, per `CLI_and_API.md`'s footprint reasoning.
- **What's genuinely new vs. what's reused**: the parse/align/diff/checksum/report-building logic is 100% the existing browser engine, completely unmodified — these packages add zero new diffing logic of their own, only automation/CLI/HTTP plumbing around it.
- **Not yet built** (see GAP_ANALYSIS.md/PLAN.md for the full list): `batch`/`report`/`merge` CLI subcommands and their API equivalents, stateful review-session API endpoints, multipart file upload (API is base64-JSON-body only), server authentication for a non-loopback bind, the native `@pptxdiff/core` engine, and an MCP server. Git integration (below) closed the one gap that WAS the headline motivating use case.
- **Testing (Red/Green TDD)**: 98 assertions across `pptxdiff-cli`'s Phase-1-diff/checksum test files and 39 across `@pptxdiff/server` — the `*_e2e.mjs`/`*_diff_checksum_cli.mjs`/`test_server_bin.mjs` files are genuine end-to-end tests (real headless browser, real HTTP requests, a real spawned CLI/server process, real `docs/assets/sample_before.pptx`/`sample_after.pptx` fixtures), not mocks or injected doubles. Every CLI flag (`--json`/`--out`/`--quiet`/`--timeout`) and both bin/ entrypoints (`pptxdiff-cli.js` AND `pptxdiff-server.js`) are covered by at least one real-spawn test, not just an injected-I/O unit test — closing a coverage gap found by explicitly auditing "did every ad-hoc behavior verified during development end up as a committed regression test" (it hadn't, for `bin/pptxdiff-server.js` and for `--out`/`--quiet`/`--timeout`'s real-process behavior). Three real bugs were found and fixed via genuine RED failures during development, not anticipated up front — all recorded in WISDOM.md: a race between the app's own default-sample auto-load and a real upload, a CSS-attribute-selector that could never match because React sets inline styles via the DOM property not the HTML attribute, and a request-body-size-limit implementation that killed the socket before its own 413 response could be written.

## 31. Git integration — `textconv`/`difftool`/`install-git-integration` (added this session)
- **What it does**: three more `pptxdiff-cli` subcommands wire `*.pptx` into `git diff`/`git difftool`, closing the headline motivating use case for the whole headless-CLI/API effort ("use pptxdiff as the diffing tool for `*.pptx` files in git" — see `docs/.scrolls/CLI_API_DESIGN.md` §7).
  - `pptxdiff-cli textconv <file.pptx> [--json] [--out <file>]` — a git `textconv` driver: prints one deck's plain-text content (real shape text + speaker notes, per slide) so `git diff`/`git log -p` can line-diff it instead of showing "Binary files differ." Exit `0` on success, `2` on error — no "differences found" concept for a single file. `--json` prints the raw per-slide `{index, shapeTexts, notes}` array instead of formatted text.
  - `pptxdiff-cli difftool <local.pptx> <remote.pptx>` — a git `difftool` driver: opens a real, **visible** browser window with both files loaded (uploaded the same way `diff` does) and blocks until the user closes it, matching git's `difftool.<name>.cmd` contract of waiting for the configured command to exit before moving to the next changed file.
  - `pptxdiff-cli install-git-integration [--global]` — wires both of the above into git for a repo in one step: appends `*.pptx diff=pptxdiff` to `.gitattributes` (idempotent, preserves existing content) and sets `diff.pptxdiff.textconv`/`difftool.pptxdiff.cmd` via real `git config` calls — local `.git/config` by default, `--global` (the flag itself is the consent — no separate interactive prompt, staying scriptable) writes `~/.gitconfig` instead, affecting every repo on the machine. Fails clearly (exit `2`) when not run inside a git repository.
- **How `textconv` gets deck text with no existing UI/export for it**: every export the app already has is diff-shaped (comparing two decks — see `buildJsonReport()`); nothing answers "what does this ONE deck's text say." Rather than reimplement extraction or add new UI to `index.html` for CLI-side plumbing, `lib/automation.js`'s `extractDeckText()` reaches the LIVE mounted component instance via React-fiber-traversal (`findLogicInstance()` — walks non-enumerable `__reactFiber$...` keys up to `stateNode.logic`, since the DC runtime wraps the logic instance in a `StreamableComponent` React wrapper) and calls its real, unmodified `shapeText()` method plus reads its already-parsed `state.slidesA` directly — zero reimplementation, same guarantee of never drifting from the app's real parser. `lib/textconv.js`'s `formatDeckText()` (pure) renders the extracted data into the final text.
- **How `difftool` differs from every other automation function**: `launchApp()` gained a `headless` option (default `true`, unchanged for `diff`/`checksum`/`textconv`); `openDifftool()` launches with `headless:false` and deliberately does NOT close the browser itself — it returns `{browser, waitUntilClosed()}`, and `waitUntilClosed()` resolves once Playwright's `disconnected` event fires (a fresh, dedicated browser instance per call means closing its one window closes the whole process, firing that event reliably).
- **Testing (Red/Green TDD)**: 82 new assertions across 7 test files (`test_textconv_format.mjs`, `test_git_integration_pure.mjs`, `test_git_integration_e2e.mjs`, `test_difftool_e2e.mjs`, `test_difftool_cli.mjs`, `test_install_git_integration_cli.mjs`, plus additions to `test_automation_e2e.mjs`/`test_cli_core.mjs`/`test_diff_checksum_cli.mjs`) — `pptxdiff-cli` package total now 182 assertions (was 98). `test_difftool_e2e.mjs`/`test_difftool_cli.mjs` need a real display (or `xvfb-run -a` in a sandbox/CI) since `difftool`'s whole point is a visible window — split into a separate `npm run test:difftool` script, not part of the default `npm test`, so a contributor/CI runner without a display doesn't fail the main suite for an unrelated reason. `test_git_integration_e2e.mjs`/`test_install_git_integration_cli.mjs` run against real temporary git repositories (`git init`, real `git config` calls, a fake `HOME` for isolated `--global` testing) — no browser needed for any of git integration except `difftool` itself.
- **A real bug found and fixed via a genuine RED test failure, not anticipated up front**: `buildGitConfigCommands()` originally produced `git --global config ...` (an invalid top-level flag git rejects with "unknown option: --global") instead of the correct `git config --global ...` (`--global` is a flag to the `config` subcommand). Caught only by actually invoking real `git` against a real temp repo — the pure unit test written alongside the implementation had encoded the same wrong assumption and gave no warning on its own. See WISDOM.md's new trap entry.

## 32. Homebrew formula for `pptxdiff` (added this session)
- **What it does**: `src/packages/pptxdiff-brew/Formula/pptxdiff.rb` is a Homebrew formula so `pptxdiff` can be installed with `brew install` instead of only `npm install -g pptxdiff`. It wraps the root `pptxdiff` npm package — already published to the npm registry at `0.7.0`, unlike `@pptxdiff/cli`/`@pptxdiff/server`, which are still monorepo-local.
- **Why no `resource` blocks were needed**: the published tarball's `package.json` has zero runtime `dependencies` — every third-party library the browser app needs (React, ReactDOM, Babel, JSZip, `pptx-renderer`, the Spectral font) is already vendored under `src/pptxdiff/vendor` and shipped as part of the npm package itself (see `files` in the root `package.json`). The formula only needs `depends_on "node"` plus `npm install` into `libexec` (`std_npm_install_args`) and a `bin.install_symlink` of the package's one bin entry (`pptxdiff` → `bin/cli.js`) — no separate dependency resolution, no vendored-dependency-in-Homebrew concern beyond what `SECURITY_ANALYSIS.md` already covers for the npm package itself.
- **Pinned version + checksum**: `url` points at `https://registry.npmjs.org/pptxdiff/-/pptxdiff-0.7.0.tgz` (the real, currently-published tarball, fetched and hashed directly rather than guessed), `sha256 "b7bc10ffee012efa1d4914e53b264b338dfbb15dea0714f31c6f664023bff272"` verified against both the locally-downloaded tarball and the npm registry's own reported shasum (converted from the registry's sha1 `dist.shasum` by hashing the actual downloaded bytes, not trusted blindly). A `livecheck` block (`strategy :npm`) lets `brew livecheck` detect future version bumps automatically once this formula is tapped somewhere `livecheck` actually runs.
- **`test do` block exercises the real installed binary**: `pptxdiff` binds an OS-assigned loopback port and never exits on its own (it starts a server, tries to open a browser, and waits) — the test spawns it via `IO.popen`, reads its first stdout line (`pptxdiff running at http://localhost:<port>`) to recover the actual port rather than guessing one, curls that URL, asserts real HTML comes back, then kills the process. This proves the installed binary actually serves the app, not just that the formula's Ruby parses.
- **Not yet a real tap**: Homebrew resolves a short tap name (`brew tap sugatoray/pptxdiff`) to a repo literally named `homebrew-pptxdiff` with formulae at that repo's own `Formula/` directory — not a subdirectory of this monorepo. For now, install directly from this formula file (`brew install --formula src/packages/pptxdiff-brew/Formula/pptxdiff.rb`, or the same path via a raw GitHub URL); a dedicated `sugatoray/homebrew-pptxdiff` tap repo that vendors this same formula is the natural next step, not built this session. See `src/packages/pptxdiff-brew/README.md` for the full install/bump instructions.
- **Real `brew` genuinely attempted, not just skipped**: two separate real attempts were made in a follow-up session. As root (this sandbox's only user), `brew` refuses outright with no override flag in current Homebrew. Under a freshly-created unprivileged user (`useradd -m`, a direct clone of `Homebrew/brew`), `brew` itself ran, but installing anything requires Homebrew's portable-ruby from `ghcr.io`, and this environment's outbound proxy returns `403` for that host (confirmed via `$HTTPS_PROXY/__agentproxy/status`, per the environment's own instructions — not assumed). Both the temp user and the Homebrew clone were cleaned up afterward. This is a genuine environment wall (network policy), not a defect in the formula.
- **Red/Green TDD given that wall**: `src/packages/pptxdiff-brew/test_formula.mjs` (`npm test` from that directory) proves everything that CAN be proven for real here instead of settling for a syntax-only check. It (1) regex-parses the formula's structure (`parseFormula`, pure — url/sha256/license/homepage/`depends_on "node"`/`std_npm_install_args`/`bin.install_symlink`/`test do`/livecheck-npm-strategy all present and well-formed), (2) runs real `ruby -c` on the formula file, (3) downloads the REAL pinned tarball and verifies its sha256 against the formula's pin, (4) cross-checks the pinned url/version/bin-field/zero-dependencies claim against the npm registry's own metadata for that version, and (5) **replays the formula's own `install` method's exact command** (`npm install --global --prefix=<libexec> --verbose --no-progress`, matching `std_npm_install_args`) against the real extracted tarball, symlinks the result exactly like `bin.install_symlink Dir["#{libexec}/bin/*"]` does, then runs the real resulting `pptxdiff` binary and curls it — the same functional proof `brew test` would give, just orchestrated by this script instead of `brew` itself.
- **Demonstrated genuine RED before GREEN**: temporarily corrupted the formula's `sha256` pin and deleted its `depends_on "node"` line, ran the test, confirmed exactly those 2 of 18 assertions failed (16/18) — including the `sha256` mismatch reporting the correct real value — while every other check, including the real `npm install`/spawn/curl against the live tarball, stayed green; restored the formula, confirmed `ruby -c`/`diff` showed it byte-identical to the pre-break version, and reran to confirm 18/18.
- **Still not run through real `brew` itself**: `test_formula.mjs` proves the formula's pin correctness and replays its install mechanics faithfully, but it cannot substitute for `brew audit`'s own style/convention linting (desc length, formula-file naming conventions, etc.) or for `brew`'s own dependency-resolution behavior. Running the real Homebrew commands on a machine that has a working `brew` (and unblocked `ghcr.io` access) is still the next verification step before treating this as fully done (see GAP_ANALYSIS.md).

## 33. Homebrew tap sync automation: `sync-tap.mjs` + `.github/workflows/sync-homebrew-tap.yml` (added this session)
- **What it does**: automates the "keep the formula's `url`/`sha256` pin current, and keep a separate `sugatoray/homebrew-pptxdiff` tap repo synced to it" work that §32 documented as a manual, hand-edited procedure. Direct follow-up to a user question about symlink/submodule/subtree approaches for the tap repo — none of those three actually work for a real published tap (a cross-repo symlink has nothing to resolve against after a plain `brew tap` clone; a submodule needs `git submodule update --init`, which `brew tap` never runs; a subtree copy is real but static, no propagation without a manual re-pull), so the shipped answer is a CI-driven sync script instead — the pattern real Homebrew taps actually use.
- **`src/packages/pptxdiff-brew/lib.mjs`** (new): shared pure/network helpers factored out of `test_formula.mjs` — `parseFormula`, `fetch`, `sha256hex` (moved, not duplicated) plus two new ones: `resolveNpmVersion(packageName, version)` (resolves `"latest"` via the registry's `dist-tags`, otherwise fetches that exact version's metadata) and `updateFormulaPin(rubySource, {url, sha256})` — pure, regex-replaces only the `url`/`sha256` lines, leaving every other line (comments, `install`/`test do`/`caveats`, license, homepage) byte-identical, and is idempotent (reapplying already-current values returns the input unchanged).
- **`src/packages/pptxdiff-brew/sync-tap.mjs`** (new CLI, `node sync-tap.mjs [--file <path>] [--version <x.y.z>|latest]`): resolves the target npm version, downloads its real tarball, computes its real sha256, and calls `updateFormulaPin` against the target file — writing it only if something actually changed, and always reporting `changed`/`version` (also written to `$GITHUB_OUTPUT` when running under GitHub Actions, so the CI workflow can branch on it). `--file` defaults to this package's own `Formula/pptxdiff.rb`, but the same script runs unmodified against a checked-out copy of the separate tap repo's `Formula/pptxdiff.rb` in CI — one implementation, two targets, no forked logic. Verified for real against the live npm registry, not just unit-tested: a no-op run against the current (already-correct) formula printed "already up to date" with zero write; a run against a deliberately-corrupted scratch copy (wrong version + a garbage sha256) correctly restored it to the real, current `0.7.0` pin, byte-identical to the real formula file afterward.
- **`src/packages/pptxdiff-brew/test_sync_tap.mjs`** (new, network-free): Red/Green tests for `updateFormulaPin` (replace-only-those-two-lines, byte-identical elsewhere, idempotent) and `sync-tap.mjs`'s exported `parseArgs` (defaults, `--file`/`--version` overrides, throws on an unknown flag rather than silently ignoring it). `package.json`'s `test` script is now `test_sync_tap.mjs && test_formula.mjs`. Demonstrated genuine RED before GREEN: broke `updateFormulaPin`'s sha256-replacement regex so it could never match, confirmed exactly 1 of 11 assertions failed, restored it, confirmed 11/11 — and re-ran `test_formula.mjs` after the `lib.mjs` extraction to confirm the refactor caused zero regression (still 18/18).
- **`.github/workflows/sync-homebrew-tap.yml`** (new, repo root): three jobs. (1) `bump-formula` (ubuntu-latest) runs `sync-tap.mjs` + (if changed) `npm test` against this repo's own `Formula/pptxdiff.rb`, uploads it as a build artifact, and opens a PR against this repo if the pin changed — so the monorepo's source-of-truth copy itself stays current release-over-release, not just the eventual tap. (2) `brew-audit` (macos-latest, needs a real change from job 1) downloads that artifact and runs **actual** `brew audit --strict --online`, `brew install --formula`, and `brew test` against it — GitHub's macOS runners ship Homebrew preinstalled as a non-root user, so this is the genuine real-`brew` verification §32 documented this sandbox as structurally unable to perform; once this workflow runs for real, that gap closes for good. (3) `sync-tap-repo` (needs both above) checks out `sugatoray/homebrew-pptxdiff` with a `HOMEBREW_TAP_TOKEN` secret and opens a PR there with the same file.
- **Trigger choice, deliberate**: `workflow_dispatch` (manual, run right after `npm publish`, optional exact-version input) plus a weekly `schedule` as a drift-catching safety net — not a push-triggered watch on `package.json`, since npm publishing from this repo is still a manual `npm publish` step with no reliable "just published" CI event to hook without risking a race against an unpublished version.
- **Two real, named prerequisites not done this session, by design**: creating the `sugatoray/homebrew-pptxdiff` repo and adding a `HOMEBREW_TAP_TOKEN` secret to this repo are both visible, credential-bearing actions left for the maintainer to take explicitly rather than an agent taking them unprompted. Until both exist, jobs 1-2 still run and are useful standalone (keeping this repo's formula current with real macOS `brew` verification on every bump); job 3 fails at its checkout step with a clear auth/not-found error rather than doing anything silently wrong. See GAP_ANALYSIS.md/GAP_CONTEXT.md and `src/packages/pptxdiff-brew/README.md`'s "Publishing to a real tap" section.
- **Follow-up in the same session: `LICENSE` + `README.md`/`CHANGELOG.md` now travel with the sync, not just the formula.** Direct ask: "always include the main LICENSE file from the repo (also used for the pptxdiff npm package), inside the pptxdiff-brew folder," and "make sure that both the README.md and CHANGELOG.md files along with the LICENSE file ... are pushed to the homebrew-pptxdiff repo via the CI." `src/packages/pptxdiff-brew/LICENSE` is now a real copy (not a symlink — a cross-repo symlink can't resolve on the tap side, the same mechanical reason a symlink can't be used for the whole sync, see GAP_CONTEXT.md) of the root `LICENSE`, matching `pptxdiff-vscode`'s existing precedent of carrying its own `LICENSE` copy for the same "distributed as a standalone artifact" reason.
- **`sync-homebrew-tap.yml` reworked to stage and push all four files, with a new `should_sync` gate**: `bump-formula`'s "stage for downstream jobs" step now copies `Formula/pptxdiff.rb` + `README.md` + `CHANGELOG.md` + `LICENSE` into a flat `dist-tap/` staging directory (deliberately staged rather than uploading the four original paths directly, to sidestep any ambiguity in how `actions/upload-artifact`'s multi-path uploads preserve directory structure — a flat, explicitly-copied staging dir is unambiguous regardless), uploaded as one `pptxdiff-tap-files` artifact. `sync-tap-repo` copies the formula into the tap checkout's `Formula/` and the other three into its repo root, then opens one PR carrying all four. A new job-level output, `should_sync` (`steps.sync.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'`), now gates jobs 2 and 3 instead of the version-pin-only `changed`: a manual `workflow_dispatch` run always pushes the current formula+docs state to the tap — covering a docs-only edit (a README wording fix, a new CHANGELOG entry) with no version bump — while a scheduled run still only proceeds when the version pin actually changed, so a routine no-drift week doesn't spin up the macOS `brew-audit` runner or open an empty-diff PR for nothing. The "open a PR against this repo" step (job 1's own PR, about the pin specifically) still gates on `changed` alone, unchanged.
- **`test_formula.mjs` gained a new "0." check** for the three tap-sync sibling files: `README.md`/`CHANGELOG.md`/`LICENSE` exist, and `LICENSE` is byte-identical to the repo root's `LICENSE` — a real drift check, not just an existence check, so an edited root `LICENSE` that never gets copied here is caught automatically instead of silently shipping a stale copy to the tap. **Caught a real bug while writing it**: the first version's byte-identity assertion read `LOCAL_LICENSE_PATH` unconditionally inside the `&&` chain without first checking it existed, so deleting the local `LICENSE` crashed the whole test script with an uncaught `ENOENT` instead of failing that one assertion — found via the genuine RED demonstration below, fixed by adding the missing `fs.existsSync(LOCAL_LICENSE_PATH)` guard. Demonstrated three real RED states before GREEN: (1) `LICENSE` moved away entirely — confirmed exactly 2 of 22 assertions failed (existence + identity) while the other 20 (including the real npm-install/spawn/curl checks) stayed green; (2) `LICENSE` restored but then a line appended (content drift, not absence) — confirmed exactly 1 of 22 failed (identity only, existence correctly passed); (3) restored to genuinely byte-identical — confirmed 22/22. Self-test count for this file: 18 → 22.

