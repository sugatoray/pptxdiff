# GAP_ANALYSIS.md — Implementation Gaps

Concrete, testable gaps between what SPEC.md describes and a fully "real" implementation. See GAP_CONTEXT.md for the reasoning behind each.

## Rendering fidelity
- [ ] Chart/SmartArt/media parts generated by `sample-pptx.js` are minimal-valid OOXML — not guaranteed to render pixel-perfect in `@aiden0z/pptx-renderer`; some slides may show "Schematic" instead of "Rendered" badge.
- [ ] Master/layout thumbnails are schematic dashed-rect outlines only (position/size of placeholders), not actual rendered master/layout art (background fills, master-level text styles, theme colors are not drawn).
- [ ] No zoom/pan on individual slide previews (only prev/next stepping).

## Diff engine
- [ ] Theme-color (schemeClr) resolution is a hardcoded partial map (tx1/dk1/tx2/dk2/bg1/lt1/accent1 only) in `readRunFmt` — other scheme colors (accent2-6, hlink, folHlink) fall back to the shape's existing color rather than resolving the real theme value.
- [ ] Table cell formatting diff is capped at 6 shown differences + "+N more" note — no way to see all of them without exporting.
- [ ] Animation diff is a single signature-string comparison (type:target:preset joined) — doesn't diff individual animation *parameters* (duration, delay, motion path coordinates).
- [ ] No diff for: slide size/aspect ratio changes between decks, section headers (`p:sectionLst`), custom shows, comments embedded in the original pptx (PowerPoint's own comment feature, distinct from this tool's reviewer comments), OLE objects, 3D models, ink/drawing annotations.
- [ ] Hyperlink diff only covers text-run hyperlinks (`a:hlinkClick` on `rPr`) — not shape-level click-through hyperlinks (`a:hlinkClick` on `nvSpPr`/`cNvPr`) or hyperlinks on images/tables.

## Deck-level comparison
- [ ] `alignSlides`/`refineMoves` similarity threshold (0.12 match / 0.55 move) is a fixed heuristic — no tuning UI, and untested against real-world decks with many near-duplicate slides (e.g. templated report decks) where it may misalign.
- [ ] No detection of duplicated slides (same slide appearing twice in one deck) — alignment assumes roughly 1:1 content uniqueness.

## Reviewer workflow
- [ ] Merge plan is an *export* (JSON of choices) — there is no mechanism to actually apply the merge plan back into a new `.pptx` file. Full OOXML round-trip merge is unimplemented.

## Batch

## Exports
- [ ] "Shareable link" is a self-contained `data:` URL copied to clipboard — NOT a real short/hosted link. Large decks could produce a very long URL (no size-limit handling/warning). Grew larger this session (now embeds the full JSON report, not just the HTML report, so the diff status can be restored on another machine) — same pre-existing gap, more likely to matter, not a new one.
- [ ] Slack export is clipboard-only plain text — no real Slack API/webhook integration (would need OAuth + a backend, out of scope for a client-only DC).
- [ ] Email export uses `mailto:` — body length is subject to browser/OS mailto length limits; no truncation warning for large reports.
- [ ] No PPTX-native export of the diff overlay (e.g. an annotated copy of the After deck with change highlights baked in as shapes).

## Transitions
- [ ] "Transition preview" is a simplified generic crossfade swatch, not a per-type accurate animation (push/wipe/etc. all look the same motion-wise; only the label text differs).

## Testing / fixtures
- [ ] Red/Green TDD sample deck covers most categories but NOT: hyperlink-on-shape (only on text run), duplicate slides, section headers, OLE objects, real (non-dummy) audio/video content, theme-color (schemeClr) diffing beyond the basic map.
- [x] No automated test harness -- CLOSED (partial): an in-browser self-test suite (runSelfTests, triggered by a button) now covers the core algorithms plus a real merge-export round trip. Not a standalone CI-runnable suite.
- [x] `gen-sample-pptx.mjs` no longer shares a spec with `buildSample()` in `index.html` -- SUPERSEDED/CLOSED (by rewrite): the script was fully rewritten to use `pptxgenjs` instead of `sample-pptx.js`, with its own independent (simplified) shape spec. The "manually-kept-in-sync copy" drift risk described here no longer applies since there's no longer a copy relationship at all — see the new gap below for what this rewrite doesn't cover instead.
- [ ] The on-disk `docs/assets/sample_*.pptx` fixtures (via `gen-sample-pptx.mjs`/`pptxgenjs`) do NOT exercise: SmartArt/diagrams, slide transitions, embedded fonts, real video/audio media. `sample-pptx.js`'s original attempt at the first of these (invented `<a:root>` root element instead of `dgm:dataModel`, missing Content-Types Override, a `dgm:relIds` graphicFrame whose four required relationship attributes all aliased one `diagramData`-typed relationship) is confirmed to break real Microsoft PowerPoint (file becomes unopenable, not even repairable) — and removing just that one defect was NOT sufficient to make the old generator's output open in real PowerPoint either, which is why the whole generator was replaced rather than patched further. `pptxgenjs` has no API for SmartArt, transitions, or embedded fonts at all (confirmed via its type definitions), and real video/audio media embedding would require sourcing/shipping actual valid media bytes (not attempted). The in-browser "Reset to sample" deck (unchanged, still uses `sample-pptx.js`) still covers all four categories for its own narrower purpose (own tolerant parser/renderer, never downloaded as a real file).

## Accessibility / i18n
- [ ] No keyboard-only navigation audit (tab order through diff rows, approve/reject buttons, etc.).
- [x] Screen-reader labeling -- CLOSED (partial): aria-labels added to the highest-value icon-only controls (remove-reviewer, per-diff approve/reject, prev/next arrows); toast uses role=status/aria-live. Still no full audit (focus rings, tab order, every icon button).
- [ ] Single-language UI (English only); no RTL testing.

## OPEN -- unresolved bug (top priority for next session)
- [ ] "Nothing is visible in Slide Diff" -- reported blank page; eval_js/get_webview_logs in the live preview returned nothing or timed out during investigation, suggesting the page hangs rather than silently renders blank. See HANDOFF.md's OPEN BUG section for the investigation trail and next step (check _probe.html's result first, then consider a load-timeout guard around the async lib-loading Promise.all in componentDidMount).

## Newly identified gaps (this session)
- [ ] Batch filename-similarity pairing uses a fixed bigram-Dice threshold (0.3) -- not validated against real-world messy filenames.
- [ ] Reviewer @mention notifications are in-memory + localStorage only -- no cross-device delivery (by design, no backend).
- [ ] The merged .pptx export's majority-vote logic gives no UI preview of which side won before download, and no per-slide override independent of per-diff picks.
- [ ] Self-test suite runs manually via a button click; not wired to CI or a pre-commit check.

## Newly identified gaps (this session)
- [ ] Keyboard shortcuts are not discoverable beyond a small toolbar hint string -- no full shortcuts help modal, no customization, and no indication of which shortcuts are active in batch view (most are no-ops there).
- [ ] Collapse-all/expand-all in All-pairs view is a single global toggle -- no per-filter-group collapse, and collapsed state is not persisted across a reload (unlike reviewer state).
- [ ] Merge-winner preview is read-only -- it shows what pickMergeWinner will choose but does not let the user override a specific slide's winner directly from that panel (they'd need to go change individual per-diff Keep-Before/After picks first).
- [ ] Drag-to-reorder in batch upload uses native HTML5 drag-and-drop (no touch/mobile support, no keyboard-accessible reorder alternative).

## Newly identified gaps (this session)
- [ ] Notion/Confluence exports are file-download + manual-import only (by design, no backend) -- see GAP_CONTEXT.md. If real API push is ever requested, it needs a backend/OAuth flow, same as Slack/email.
- [ ] uiState in the JSON report export is one-way (export only) -- there's no 'import this report to restore UI state' path; a re-uploaded report JSON doesn't feed back into collapsedPairs/selectedPairs/mergeWinnerOverrides.
- [ ] Touch drag-to-reorder has no visual drag-preview/ghost element while dragging on touch (native HTML5 DnD gives one automatically on desktop; the manual touch implementation does not).

## Newly identified gaps (this session)
- [ ] Live push to Notion/Confluence will almost certainly fail from a real browser due to CORS (no backend proxy exists) — the feature is implemented "for real" and will work the moment a user runs it through an environment where CORS isn't blocking (e.g. a browser extension, a proxy, or if either vendor changes policy), but as shipped today it degrades to an error message pointing at the file-based export. This is NOT a bug to silently fix — a real fix requires a backend, which is out of scope unless requested.
- [x] Slack push delivery confirmation -- CLOSED (partial): switched from an opaque `mode:'no-cors'` fetch to a hidden form/iframe POST, whose load/error events distinguish 'request round-tripped' vs 'never went out'. Still not a true HTTP-status confirmation (iframe `load` fires for any response, including a 4xx/5xx from a bad/revoked webhook) -- see GAP_CONTEXT.md.
- [ ] Confluence push always APPENDS to the existing page body (via `<hr/>` + new content) rather than offering replace-vs-append as a choice.

## Newly identified gaps (this session)
- [ ] Slack iframe-based delivery confirmation cannot distinguish a genuine 2xx from an HTTP error response body that still loads in the iframe (e.g. a revoked webhook returning a 404 page) -- true confirmation would need a backend to read the actual status code.
- [ ] mergeReportPatch's conflict rule (current session always wins) is fixed/non-configurable -- a user who WANTS the imported file's decisions to win on conflict (e.g. importing a more authoritative reviewer's file) has no way to flip that preference; they'd have to Overwrite instead, losing their own unsaved-elsewhere choices.

## Newly identified gaps (this session)
- [x] Cross-deck duplicate detection -- CLOSED: `findCrossDeckDuplicates` flags an unaligned Before/After slide pair that closely resembles each other, surfaced as a separate note panel from the same-deck DUPLICATE tag.
- [x] Per-field credential persistence 'select all/none' -- CLOSED: 'Remember all' / 'Forget all' buttons added.
- [ ] The import conflict-winner choice ('current' vs 'imported') is only offered for Merge, not for the underlying per-diff mergeChoices/diffDecisions used by the separate three-way-merge-export feature — those remain untouched by report-JSON import (they were never part of parseReportForImport's scope) and don't have their own conflict-winner concept.

## Newly identified gaps (this session)
- [x] Cross-deck duplicate performance at scale -- CLOSED: maxPairs cap (default 6000 comparisons) with skip+force-override.
- [ ] Batch file selection checkboxes have no bulk action besides 'Remove selected' (no bulk move-to-other-side, no bulk re-pair).
- [ ] Reviewer list 'Remove selected' has no confirmation step — bulk-removing several reviewers at once is immediate/irreversible (aside from re-adding them manually).

## Newly identified gaps (this session)
- [ ] Selection state for diffs/comments/history is NOT cleared when switching slide pairs or reloading — a `selectedDiffs`/`selectedComments` entry from a previous pair view can look stale if a user navigates away and back (the keys are namespaced by pair/comment so they won't cause a wrong action, but old checkboxes could remain checked in a way that isn't obviously visible until scrolled to).

## Newly identified gaps (this session)
- [x] Single-comment delete confirmation -- CLOSED: now confirmation-gated (reverses the prior session's deliberate 'stays immediate' decision, per this session's explicit ask).
- [ ] Bulk reply delete has no equivalent select-all/none/bulk-delete treatment — only top-level comments got this session's (and the prior session's) selection/bulk features; replies within a comment thread have no delete capability at all yet (single or bulk).

## Newly identified gaps (this session)
- [ ] Reply delete is single-only -- no select-all/none or bulk-delete for replies within one comment's thread (unlike top-level comments, which have full bulk selection).
- [x] 'Clear decisions' scoping -- CLOSED: All / Only my votes / Specific reviewer / Specific slide range.
- [ ] Confirmation modals now number 6 total (reviewers, comments-bulk, history-bulk, comment-single, batch-file-single, decisions-clear-all) with near-identical markup repeated 6 times in the template -- a shared modal component/pattern could reduce duplication if a 7th is ever added.

## Newly identified gaps (this session)
- [ ] 'Specific slide range' scope for clearing decisions uses row POSITION in the current All-pairs alignment order, not Before/After slide numbers -- if the alignment changes (e.g. after re-uploading a deck with different content), a previously-typed range no longer refers to the same slides. Not persisted across re-alignment, which is probably correct behavior but worth noting.
- [ ] No validation/clamping shown to the user if 'from' > 'to' in the slide-range inputs beyond the pure function's internal `Math.max(from, to)` clamp -- the UI doesn't visually indicate this happened.
- [ ] The reviewer-handle text input for 'Specific reviewer' scope has no autocomplete/dropdown against the actual reviewer list -- a typo'd handle silently clears nothing (matches no keys) rather than erroring.

## Newly identified gaps (this session)
- [ ] The Sensitivity slider and per-pair Ignore list are global (apply to ALL cross-deck duplicate detection everywhere: single-pair view panel, merge preview, batch mode) -- there's no per-context override (e.g. a looser threshold just for batch mode).
- [ ] 'Duplicate slide merging' only affects Before-only (removed) slides with a cross-deck duplicate -- it does not attempt anything symmetric for After-only (added) slides that might duplicate a Before slide's content in a different way (e.g. reordered AND slightly reworded).
- [ ] Batch mode's cross-dup count is a single number per pair with no drill-down (no way to see WHICH slides duplicated from the batch results table -- would need to open that pair into the full view).

## Packaging (newly identified gaps, this session)
- [ ] `pptxdiff` npm CLI (`bin/cli.js`) opens a browser tab, not a real native window (no dock/taskbar icon, no app-like process) -- not a true "desktop app" in the Electron/Tauri sense.
- [x] The CLI required internet access at runtime -- CLOSED: React/ReactDOM/Babel-standalone/JSZip/pptx-renderer/Spectral font are all vendored locally under `src/pptxdiff/vendor/` and loaded from disk; zero CDN dependency remains for the app to boot and function. See SPEC.md §24 and GAP_CONTEXT.md for the reasoning (this reverses last session's deliberate "kept CDN-based" scope call, per this session's explicit user ask).

## Documentation site (newly identified gap, this session)
- [x] The `docs-site/` pages (`architecture.md`, `limitations.md`, `getting-started.md`, `index.md`, `faq.md`, `cli.md`, `features/rendering.md`) described the app as CDN-dependent/requiring internet access -- CLOSED: that content was written in a session that forked before the offline-vendoring work landed. Rebased this branch onto `master` (which had merged the docs-site) and corrected every stale CDN/internet-access claim across those 7 pages to match the now-vendored reality; re-verified with `mkdocs build --strict` (clean).

## Offline capability (newly identified gaps, this session)
- [ ] `pdfjs-dist` (an optional peer dependency of `@aiden0z/pptx-renderer`, used only to render embedded PDF objects inside a slide) is loaded via a runtime-constructed `import(pdfjsUrl)` string inside a Web Worker and was NOT part of this app's dependency set even before vendoring -- it remains unvendored and would fail offline if a `.pptx` ever contained an embedded PDF object exercising that code path. Narrow, pre-existing, not a regression from this session's vendoring work.
- [ ] The vendored Spectral font only includes the "latin" Unicode-range subset (matching the app's English-only UI) -- non-Latin text typed into comments/reviewer names/etc. will fall back to the browser's default serif font instead of Spectral's cyrillic/vietnamese/greek glyph coverage that the original Google Fonts CDN link would have served. Purely cosmetic (Spectral is only used for the `h1`/headings, not body text), not a functional gap.
- [x] `test_offline_capable.mjs`'s CDN-host scan was a fixed list of known hosts scanned for total absence -- SUPERSEDED (not just closed): rewritten as a per-swap-point check (see "PPTXDIFF_LITE_MODE" below) since CDN URLs are now a legitimate, gated, opt-in part of the source rather than something that should never appear at all.

## PPTXDIFF_LITE_MODE (newly identified gaps, this session)
- [ ] The env var only works through `bin/cli.js` (Node has env access; a browser doesn't) -- the "just open `index.html`"/VS Code extension paths have no way to trigger lite mode except manually appending `?lite=1` to the URL. Documented, not a bug -- see SPEC.md §25/GAP_CONTEXT.md for why this is the correct design given the constraint, not an oversight.
- [x] No UI affordance (banner/toggle) inside the app itself indicating lite mode is active -- CLOSED: an explicit "Offline Mode" corner toggle (top-right, distinctly separate from the toolbar) now shows and controls the current state in-app, per an explicit follow-up ask. See SPEC.md §25.
- [ ] `?lite=1` is all-or-nothing across all 5 vendor swap points (React, ReactDOM, Babel, JSZip, pptx-renderer, font) -- no way to mix (e.g. vendored React but CDN pptx-renderer) without hand-editing the source. Not requested; a single flag matches the actual use case (A/B against unmodified upstream, or explicitly wanting fresher CDN builds) better than partial mixing would.
- [ ] The in-app "Offline Mode" toggle's OFF (CDN/lite) rendered state — track color, thumb position, `aria-checked="false"` — was not confirmed in a live, fully-booted browser in this dev sandbox, since `?lite=1` can't complete a real boot here (the same outbound-proxy blocking noted elsewhere). The underlying logic is the identical one-line ternary pattern already proven correct for `LIB_URL`/`REACT_URL`/etc., and the ON state (default) was fully E2E-verified, but a from-scratch visual confirmation of the OFF state specifically is still owed on a machine with normal internet access.

## Documentation coverage (newly identified gaps, this session — surfaced BY the new tracking system itself)
- [x] **Diff engine limitations undocumented publicly** — CLOSED: added to `limitations.md` ("Diff engine coverage isn't exhaustive" row).
- [x] **Deck-level comparison limitations undocumented publicly** — CLOSED: added to `limitations.md` ("Deck-alignment thresholds are fixed" row).
- [x] **Batch mode limitations undocumented publicly** — CLOSED: added to `limitations.md` ("Batch filename-similarity pairing uses an untuned threshold" row).
- [x] **On-disk sample fixture gaps undocumented publicly** — CLOSED: added to `limitations.md` ("On-disk sample fixtures don't cover everything..." row).
- [x] **Accessibility limitations undocumented publicly** — CLOSED: added to `limitations.md` ("English-only UI, no full accessibility audit" row).
- [x] **Packaging limitations undocumented publicly** — CLOSED: added to `limitations.md` ("The npm CLI opens a browser tab..." row).
- [x] **Offline-capability limitations undocumented publicly** — CLOSED: added to `limitations.md` ("Offline vendoring has two known gaps" row).
- [x] **Lite-mode limitations undocumented publicly** — CLOSED: added to `limitations.md` ("`PPTXDIFF_LITE_MODE` is all 5 dependencies at once" row).
- All eight were real, pre-existing gaps in `GAP_ANALYSIS.md`/`GAP_CONTEXT.md` (the internal working-memory scrolls) that were never carried into the PUBLIC docs site. Deliberately left `missing` in the session that built the tracker (see GAP_CONTEXT.md for why), then closed with real content — sourced from this same file, not invented — in an explicit follow-up the same day. `documentation-coverage.md` is now 33/33 complete.

## Security hardening (this session)
- [x] **The VS Code extension's own static file server (`extension.js`) had the same unbound-`listen(0)` and raw-`startsWith(ROOT)` weaknesses `bin/cli.js` was hardened against** — CLOSED: ported loopback binding, `path.relative()` containment, and response headers over; verified via the existing `test_extension.js` plus a direct `isPathContained` sibling-directory check.
- [ ] **No automated check keeps `bin/cli.js` and `pptxdiff-vscode/extension.js`'s duplicated static-server logic in sync.** They are two independent copies (no shared module), so a future fix/hardening change to one can silently miss the other unless someone remembers to check by hand — see WISDOM.md's new trap entry. Not built this session (would need a small drift-check script or a shared module extraction, neither requested).

## Screenshot export (this session)
- [ ] **Screenshot captures are `.svg`, not `.png`/raster image files.** A hard, unconditional browser restriction (Chromium taints any `<canvas>` an SVG containing `<foreignObject>` is drawn onto, blocking `toDataURL()`/`toBlob()` regardless of content — see WISDOM.md's new trap entry) rules out producing real PNG bytes without a new vendored dependency (`html2canvas`). SVG files open and display correctly in any browser/most image viewers, so this is a real, working deliverable — just not the raster format a "screenshot" implies by default. Not something a code fix can close without either vendoring `html2canvas` or an equivalent, neither of which was asked for.
- [ ] **Custom `@font-face` text (this app's Spectral headings) isn't guaranteed to carry its font into the captured screenshots.** The foreignObject sandbox reliably preserves every inline `style="..."` attribute (this app's actual styling mechanism) but not a separate `<link>`/`@font-face` declaration — headings may fall back to a default serif in the captured SVG even though they render in Spectral live. Not investigated further; would need inlining the font as a data URI directly into the captured SVG's own `<style>`, not attempted this session.

## Headless CLI / Web API — Phase 1 (this session)
- [ ] **Every CLI/API invocation pays a real headless-Chromium boot cost (~1-2s).** Phase 1 (per CLI_API_DESIGN.md's phased plan) deliberately reuses the browser app as-is via `playwright-core` automation rather than building the native `@pptxdiff/core` engine extraction (Phase 2) — the right tradeoff for shipping/validating the command/endpoint surface fast, but not yet the fast/light shape needed for a git `textconv` driver invoked on every `git log -p`. Not started this session; the extraction is scoped and reasoned about in the design doc, not attempted.
- [x] **No git integration (`textconv`/`difftool`) yet** — CLOSED this session: `pptxdiff-cli textconv`/`difftool`/`install-git-integration [--global]` ship. See SPEC.md §31.
- [ ] **`textconv`'s text extraction covers only shape/placeholder text and speaker notes**, not table cell text, chart data labels, or SmartArt text — narrower than the app's own diff engine, which does cover tables/charts/SmartArt for the GUI's diff view. A git `textconv`-diffed `.pptx` won't show a change that's ONLY inside a table cell or chart data point. Not attempted this session; would need `extractDeckText()` extended to read `slide.tables`/`slide.charts`/`slide.smartArt` in addition to `slide.shapes`.
- [ ] **`install-git-integration` only writes a repo-local `.gitattributes`**, never a global attributes file (`core.attributesFile` in `~/.gitconfig`). Combined with `--global` git config, a user CAN get the driver definition applied machine-wide, but the `*.pptx diff=pptxdiff` association itself still needs a per-repo (or manually-configured global) `.gitattributes` — there's no `install-git-integration --global` mode that sets up a true zero-per-repo-setup experience. Not attempted; would need deciding whether/how to touch `core.attributesFile`, a real design question not just an oversight.
- [ ] **`@pptxdiff/server` has no authentication.** `startServer()` binds to loopback by default (matching `bin/cli.js`'s precedent), but passing `--host` to bind elsewhere is an unguarded opt-in — the design doc's API-key-for-non-loopback requirement (§8) is not implemented. Documented explicitly in the package's README rather than left as a silent trap.
- [ ] **`@pptxdiff/server` only accepts file content as base64 in a JSON body**, not `multipart/form-data`. Simplest wire format with zero new dependency; real multipart support was explicitly scoped out as a fast-follow, not attempted.
- [ ] **`@pptxdiff/server`'s `/docs` page loads Swagger UI assets from a CDN.** The OpenAPI spec itself (`/openapi.json`) is served locally and works offline, but the browser UI at `/docs` needs internet access unless/until Swagger UI assets are vendored or replaced with a tiny local renderer.
- [ ] **Only `diff` and `checksum` are implemented**, in both the CLI and the API. `batch`, `report` (the non-JSON export formats — HTML/MD/CSV/Notion/Confluence/PDF/screenshot), `merge`, and the design doc's stateful review-session API endpoints are all designed (CLI_API_DESIGN.md §5/§8) but not built.
- [ ] **No MCP server** (`pptxdiff-cli mcp`, CLI_API_DESIGN.md §9) — the more idiomatic AI-agent integration point than shelling out to the CLI or hand-rolling HTTP calls. Not started; `--json` output + the API's request/response shapes are the current AI-agent integration surface.
- [ ] **Not published to npm.** Both packages depend on their monorepo siblings via `file:` protocol (documented in each README) rather than a real published semver range — intentional for local development, but a real blocker before either package can actually be `npm install`ed by anyone outside this repo.

## Standalone native binaries (this session, switched from Node SEA to `@yao-pkg/pkg` mid-session)
- [ ] **Windows `.exe` is unsigned; macOS binary is ad-hoc signed only (no Developer ID).** No code-signing certificate exists for this project — Windows SmartScreen and macOS Gatekeeper will both warn on a freshly-downloaded copy. Documented per-OS in `src/packages/binaries/pptxdiff-{win,mac}/README.md`; a real fix needs a paid cert (Apple Developer ID + Windows Authenticode), not a code change.
- [ ] **macOS is not cross-compiled** — `pkg` genuinely can cross-compile a macOS binary from Linux, but the result can't be codesigned there (`codesign` is macOS-only), and a completely unsigned binary may not even launch on Apple Silicon. `.github/workflows/binaries.yml` therefore still runs macOS on a real `macos-latest` runner (`build-mac`), separate from the `build-linux-win` job that genuinely does cross-compile both those targets from one Linux host. This is a real, load-bearing constraint, not leftover caution from the SEA-based version.
- [ ] **Not yet attached to GitHub Releases.** The CI workflow uploads each OS's binary as a workflow artifact (downloadable from the Actions run page) but nothing wires a release-tag push to attach them to an actual GitHub Release yet — a real, small follow-up (e.g. `softprops/action-gh-release` on `release: types: [published]`), not attempted this session.
- [ ] **x64 only, no native arm64 build for any OS.** Matches the scope of the original (SEA-based) version; an Apple Silicon Mac runs the x64 binary via Rosetta 2. `pkg` does support arm64 targets (`node22-linux-arm64`, `node22-macos-arm64`, etc.) if this is ever asked for — not attempted, to keep this session's scope to what was requested.
- [ ] **macOS/Windows builds are unverified end-to-end in this sandbox** — only the Linux build was actually run and its binary actually executed (built, launched, served `index.html`/`support.js`/`vendor/*` via real HTTP requests) — under both the original SEA mechanism and, again, after switching to `@yao-pkg/pkg`. The Windows binary WAS structurally produced here (real `.exe`, verified during exploration before writing the final `build.mjs`) but not run (no Windows host in this sandbox); the macOS codesign branch is exercised for the first time whenever CI's `build-mac` job runs, not locally.

## Content checksum (this session)
- [ ] **The content checksum shows "unavailable" under the plain `file://` open path.** `crypto.subtle` (native Web Crypto, no new dependency) requires a secure context — guaranteed under the CLI's `http://localhost` default, not guaranteed under this project's other documented launch path (`git clone` + open `index.html` directly). Handled honestly (checked once at boot via `cryptoSubtleAvailable`, shown as "unavailable (requires a secure context)" rather than a wrong/fabricated hash or a permanently-stuck "computing…"), but not worked around — would need either a pure-JS SHA-256 fallback (a real new dependency, or a hand-rolled implementation neither asked for nor free of its own correctness risk) or accepting the gap under that one launch path.
- [ ] **The checksum's excluded-parts list (`docProps/core.xml`, `docProps/app.xml`, `docProps/thumbnail.*`) is a fixed, hardcoded set, not user-configurable.** If a future real-world `.pptx` turns out to carry other non-content, save-time-varying metadata this project hasn't seen yet, it would need a code change to exclude, not a setting. Not speculatively generalized ahead of an actual observed case.
